# 스마트 로봇 프로젝트 명세서
### LG Physical AI 체험형 부스 레퍼런스 — "토마토 따기 로봇"

작성일: 2026-06-26
의뢰: 포너즈 → 박기호 / 공동진행: 용용이님

---

## 1. 개요

LG AI 컨퍼런스 체험형 부스에 들어갈 **Physical AI 데모 작품**을 제작한다.
화면 속 AI가 아니라 **로봇이 직접 보고(인식) 움직이는(액션)** 피지컬 AI를 보여주는 것이 목표다.

핵심 서사: **"언어 → 인식 → 행동"**
음성 명령을 LLM이 받아 → 로봇이 카메라로 익은 토마토를 찾고 → 직접 따서 바구니에 담는다.

핵심 메시지: **"쉽게 구할 수 있는 로봇팔로도 피지컬 AI가 가능하다."**

---

## 2. 목표 / 범위

- 부스 관람객 앞에서 **안정적으로 반복 시연**되는 데모.
- 정밀도·완성도보다 **개념 증명(PoC)**과 서사 전달이 우선. 초기 단계라 투박해도 됨.
- 비즈니스 가치가 보이는 작품(수확 자동화 맥락).

비범위(이번엔 안 함): 자유주행, 임의 환경 인식, 정밀 그리핑 최적화.

---

## 3. 설계 원칙 (안정성 최우선)

부스 데모는 **결정적(reliable)** 이어야 한다. 그래서 "AI"는 딱 두 층에만 둔다.

1. **익은 열매 인식** — 색 기반 검출 (빨강=수확 / 초록=미수확)
2. **자연어 → 함수 계획** — LLM(Claude) 함수콜 오케스트레이션

나머지 저수준 동작(이동·집기)은 **전부 하드코딩/결정적**으로 처리해 실패 요인을 최소화한다.

- 무대: **나무 모형 3개 일렬**, 정해진 위치에 열매 고정(자석으로 위치 표시).
- 이동: 메카넘 베이스는 **직선 운동만**으로 나무 앞까지 이동(자유주행 X).
- 따기: 픽 동작은 **사전 정의된 시퀀스**로 재생.

---

## 4. 시스템 아키텍처

```
[음성 명령]
    │
    ▼
[Claude LLM · 함수콜 오케스트레이션]  ── 자연어를 로봇 스킬 함수 호출로 변환
    │
    ├─ drive_to_tree(id)      나무 앞으로 직선 이동
    ├─ scan_tree()            카메라로 열매 검출 → [위치·익음] 목록 반환
    ├─ pick_fruit(id)         지정 열매 따기 (하드코딩 시퀀스)
    ├─ place_in_basket()      바구니에 담기
    └─ home()                 기본 자세 복귀
    │
    ▼
[로봇 하드웨어 — 팔 + 이동베이스 + 카메라]
```

예시 명령: **"수확 가능한 모든 열매를 수확해"**
→ Claude가 나무를 순회하며 `drive_to_tree → scan_tree → (빨강만) pick_fruit → place_in_basket` 반복 실행.

---

## 5. 하드웨어 구성

| 구분 | 품목 | 비고 |
|---|---|---|
| 로봇팔 | LeRobot **SO-ARM101 Max** (Feetech 서보) | 조립형, 텔레오퍼레이션 + 모방학습 ACT 지원 |
| 연산보드 | **NVIDIA Jetson Orin Nano Super** 개발자킷 | 에지 AI, 비전(색검출/YOLO) |
| 이동베이스 | RC **메카넘 휠 프레임** + 모터컨트롤러(아두이노 우노) | 직선 이동 전용 |
| 케이스 | 젯슨 오린 나노 NX 케이스 | — |
| **카메라 (미구매·필수)** | USB 웹캠 2대 (전면 + 손목, 로지텍 C920류) | **7/9 출국 전 주문 필요** |
| 전원 (검토중) | UPS / 배터리 (5V 5A↑, 팔·모터 동시 구동) | 순간 전류 여유 필요, PD/QC 권장 |

배송지: 경기 수원시 팔달구 창룡대로103번길 92, 매향동 201호 (16254) · 박기호 010-6891-7112

---

## 6. 소프트웨어 구성

### 6.1 스킬 함수 API (초안)
- `drive_to_tree(id)` — 나무 id 앞까지 직선 이동
- `scan_tree()` → 열매 목록 `[{id, position, ripe}]`
- `pick_fruit(id)` — 지정 열매 픽 시퀀스 재생
- `place_in_basket()` — 바구니에 적재
- `home()` — 홈 포지션 복귀

이 함수들을 Claude에 **tool**로 노출 → 음성/텍스트 명령을 복합 작업으로 계획·실행.

### 6.2 인식
- 색 코딩 기반 검출(빨강=익음 / 초록=안익음). 단순 색검출이라 신뢰성 높음.
- (선택) 시간 여유 시 YOLO·뎁스(리얼센스 D435)로 고도화.

### 6.3 ⚠️ 오프라인 폴백 (필수)
- Claude API는 인터넷 필요 → 부스 와이파이 불안정 대비.
- **완전 오프라인 자동 시퀀스(데모 모드)** 를 폴백으로 준비. 망 없이도 전체 수확 데모가 한 번에 돌아가게.

---

## 7. 데모 시나리오

1. 관람객/진행자: "익은 토마토 다 따줘"
2. (온라인) Claude가 명령 해석 → 스킬 함수 계획 수립 / (오프라인) 데모 모드 자동 시퀀스
3. 나무 1 → 직선 이동 → 스캔 → 빨강 열매 픽 → 바구니
4. 나무 2, 3 반복
5. 완료 후 home 복귀

---

## 8. 일정 / 역할

- **마감: 2026-08-초** (가용 약 한 달~한 달 반)
- **박기호 독일 체류: 7/9 ~ 7/27** → 이 기간 직접 작업 불가
  - 출국 전: 스펙 확정 + **카메라 등 부품 발주 완료**
  - 체류 중: 용용이님 진행
  - 귀국(7/27) 후: 통합·튜닝·리허설
- 남은 작업 순서: 조립 → 제어 시스템 → 위치 인식 → 수확 알고리즘 → 폴백/리허설

---

## 9. 예산

- 총예산: **300~400만원** (단위 확인 필요)
- 현재 집행: 약 130만원 (팔·젯슨·베이스·케이스) → 여유 있음

---

## 10. 미확정 / 확인할 것

- [ ] 카메라 사양 확정·발주 (최우선, 7/9 전)
- [ ] 전원/UPS 사양 확정 (5V 5A↑, 팔+모터 동시)
- [ ] 예산 단위(300~400만원) 확인
- [ ] 용용이님 역할·독일 기간 중 진행 범위
- [ ] 음성인식이 데모 필수 요소인지
- [ ] 부스 데모 날짜(컨퍼런스 주차)
- [ ] LeRobot 셋업 환경 — Jetson(ARM64) vs PC(x86) 결정

---

*본 명세서는 2026-06-24 확정 컨셉/아키텍처 기준 초안입니다. 변경 시 갱신.*
