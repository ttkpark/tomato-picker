# 최상위 목표 (2026-09-17 ~ 10-02, 15일)

## 한 줄
**`v2.0.0-ros.2` 졸업** — 좌표로 보내면 집게가 거기로 간다.

## 합격 기준 (docs/ros2-이행계획.md §3)
1. 링크 길이·마운트를 **자로 잰 실측값**으로 URDF와 `kinematics.py`가 일치한다.
2. 손-눈 보정 **잔차 ≤ 15mm** (`handeye_resolve.py`가 찍는 residual).
3. 임의의 자리에 둔 표적을 `/arm/move_to_point`로 **5회 중 5회** 스탠드오프까지 도달.
4. PC 자체검증 **네 벌이 전부 RC=0** — `ros2/tools/ros_selfcheck.py` · `tools/handeye_check.py` ·
   `tools/eye_check.py` · `tools/arm_cartesian_check.py`. **종수는 기준이 아니다**(검사를 늘리면
   숫자가 낡는다) — 늘어난 검사가 통과하는 것까지 포함해서 넷 다 RC=0이어야 한다.
   참고로 2026-09-18 02:0x 기준 156·45·90·53 = 344종(01:00의 276종에서 늘었다 —
   ros_selfcheck +48[단계·쪼개기·표적], arm_cartesian_check +20). **이 숫자는 참고일 뿐
   기준이 아니다.** ⚠ 종수가 늘어도 그 검사가 *실패할 수 있어야* 뜻이 있다 → T39.
5. 위 넷이 되면 `tools/tag.sh v2.0.0-ros.2 "..."`.

## 지금 알려진 최대 장애물
- **평행이동 오차가 실측의 3~5배** (docs/인수인계-2026-09-04.md, 미해결).
  회전은 브래킷 유격 고정 + 재캘리로 해결됐다.
  ⚠ **그런데 '실측 75~80mm'라는 기준 자체가 의심스럽다**(2026-09-17 플래너).
  `ros2/tools/handeye_from_axes.py`를 젯슨 `~/joint_axes.json`(09-01)에 돌리면
  `t_tool_cam=(-98.2, 33.5, 57.8)mm · |t|=119mm · 거리잔차 7.8mm · 회전 0.68°`가
  나온다. 이행계획 §"camera_at"이 적은 실물 배치(뒤 7~8cm · 위 7~8cm)의 벡터
  크기는 √(80²+80²)≈113mm이므로 **119mm 쪽이 배치와 맞는다.** 09-04 내내 쓴
  `--expect-t-mm 60 100` 게이트가 옳은 해를 버리고 있었을 수 있다 → T8에서 판정.
  (⚠ 이 119mm는 **브래킷 재접착·재교시 전** 데이터다. 재수집 = T10.)
- 남은 수는 **넣을 실측 숫자**이지 코드가 아니다 — §8-1이 "새로 짜야 한다"고 한
  `--fix-t`는 이미 있다(`handeye_resolve.py:334`, 커밋 05f6639). → T9(사람 실측).
- 카메라 링크 관절(롤)을 만지면 `~/grip_uv.json`이 무효가 된다(개발일지-2026-09-11 §1).

## 하지 않을 것
- 기존 스택(`src/tomato_picker/`) 기구학 재작성. 라이브러리로 계속 쓴다.
- ROS를 웹 서비스에 붙이기.
- master에 `-ros.N` 태그, ROS 브랜치에 `v1.x` 태그.

## 갱신 규칙
플래너만 이 파일을 고친다. 고칠 때는 **무엇이 사실로 바뀌어서** 고치는지 한 줄 남긴다.

## 사실이 바뀌어서 고친 기록
- **2026-09-17 (플래너, 사이클 1)** — ① 기준4(175종)는 **오늘 PC에서 충족**한다:
  handeye_check 45/45 · eye_check 60/60 · ros_selfcheck 70/70 · arm_cartesian_check
  33/33. 단 `.venv`에 `pyyaml`이 없어 ros_selfcheck가 죽고 있었고 그걸 통과로
  오독할 뻔했다(T11). ② 기준1의 링크 길이는 2026-08-31에 이미 실측됐고
  `ros_selfcheck`가 yaml↔코드 일치를 **강제한다**(1mm 틀면 실패 확인). 남은 미실측은
  `so101_geometry.yaml`의 `mount.x: 60.0` 하나뿐. ③ 최대 장애물 절에 위 119mm 근거를
  추가했다 — 목표를 낮춘 게 아니라 **기준으로 삼던 75~80mm가 틀렸을 가능성**을 적었다.

- **2026-09-18 (플래너, 사이클 8)** — ① **기준4의 '175종'을 지웠다**(T14). 검사를 늘릴 때마다
  기준이 낡아 '통과'의 뜻이 흐려지던 것을 '네 벌 전부 RC=0'으로 바꿨다. 목표를 낮춘 게
  아니라 **늘어난 검사도 포함하도록 올린** 것이다(175 → 실제 243종이 모두 초록이어야 한다).
  ② **하루를 통째로 막고 있던 것이 사람 손이 아니었다.** `/dev/shm/d405_color.jpg`의 회색
  평균이 **7.2/255 · 60백분위 7.0 · 최댓값 42**다(00:06 실측). 사람이 볼 수 있는 방에서
  이 숫자면 조명이 아니라 **발행기 노출**이다 — `tools/depth_cam.py`는 컬러 센서에
  exposure/gain/auto_exposure를 하나도 안 건다(`set_option`이 `holes_fill` 하나뿐, :147).
  그래서 09-17 테스터가 내린 "4점 마커판이 시야 밖"이라는 판정은 **근거가 못 된다** —
  `find_dots`의 contrast 게이트(`target_check.py:112`)는 동적 범위 0~42에서 마커가
  눈앞에 있어도 못 넘는다. → T18(노출) → T19(재판정) → T9 → T13.
  ③ **기준3은 아직 '모름'인데, 지금 당장 잴 수 있다**는 것을 놓치고 있었다.
  `ros2/tools/move5_check.py`는 좌표를 받지 카메라 검출을 받지 않으므로 손-눈 보정·거울
  논란(§21)과 **무관하게** TF/IK/관절 중 어디가 깨지는지 오늘 가른다. → T21.

- **2026-09-18 (플래너, 사이클 15)** — ① **기준3이 '모름'에서 '0/5'로 바뀌었다.** T21(사이클 14)이
  실기로 쟀다: 5회 전부 실패, 팔은 **한 번도 안 움직였다**. 원인은 TF도 IK도 아니라
  **시작 자세**다 — 시작 자세와 저장된 프리셋 1·2·5의 `signed_radius`가 -136~-414mm라
  `cartesian.py:470`의 가드(90mm)가 좌표 이동을 거절했다. 기록
  `docs/시험기록/move-to-point-2026-09-18.jsonl`에 `stage=tf`로 적혀 있지만 **이건 분류
  버그다**(가드 문구에 '좌표'가 들어 있어 `move5_check.py:146`의 문자열 판정에 걸린다) →
  T28에서 고친다. **기준3은 아직 한 번도 진짜로 시험된 적이 없다**고 읽어야 한다.
  ② **그 가드를 빠져나오는 도구는 이미 있다** — `ros2/tools/arm_extend.py`(09-03,
  젯슨에도 배포돼 있다). 관절공간으로 12°씩 가며 구간마다 바닥·사거리·관절한계를 검사하고,
  기본 목표가 r≈250mm(가드의 2.7배)다. **새 프리셋을 가르칠 일이 아니다**(T26 note).
  T20(거울 판정)도 같은 가드에 막히므로 같은 선행이 필요하다.
  ③ 기준4는 오늘도 충족: 45·90·108·33 **전부 RC=0**(01:00 PC 실행).
  ④ **사람 손 대기가 넷으로 굳었다**(T9·T10·T12·T23). 넷 다 blocked로 옮겼다 — 자동 역할이
  집었다가 되돌리며 사이클을 버리던 것을 막기 위해서다. 넷이 없어도 T26→T20→T28→T29는 돈다.


- **2026-09-18 (플래너, 사이클 22)** — ① **기준1의 09-17 채점이 틀렸다.** 그때 나는 "링크
  길이는 실측+강제, 남은 미실측은 `mount.x` 하나뿐"이라고 적었다. 사이클 19 감사(T29)가
  대조해 보니 `mount.x/y/z/yaw_deg` **어느 하나도 자동으로 강제되지 않는다** —
  `tf_check.py:136-143`은 문구로 "yaml과 같은가"라 해놓고 실제론 "0이 아니다"만 보고,
  그 파일은 도커가 있어야 돌아 PC 4종 게이트에 들어 있지도 않다. 즉 기준1은
  **링크 길이만 합격**이고 마운트 4개는 실측도 강제도 없다. → 강제 = T32(builder),
  실측 = T12(사람), 기준선 = T38(tester).
  ② **기준3을 막는 것이 세 번 옮겨갔고 지금은 '경로'다.** 자세 가드(T21·T26) →
  한 걸음 상한(T30) → 표적이 가동범위 밖(T31) → **직교 직선 보간의 첫 걸음이
  관절한계에 걸린다**(사이클 20, `elbow_flex −124~−133`). 목표 사전검사는 이제
  **5/5 통과**한다. 점수는 여전히 **0/5**. → T33.
  ③ **아직 아무도 재지 않은 숫자 하나가 기준3의 천장을 정한다** — 같은 관절값으로
  돌아올 때 TCP가 몇 mm 흔들리는가. 기준3의 15mm는 FK 좌표에서 재므로, 서보
  반복오차가 15mm보다 크면 **코드로는 절대 5/5가 안 나온다**. 카메라도 마커판도
  사람도 필요 없다 → T36(p1). 결과가 15mm를 넘으면 그때는 이 목표의 기준3을
  고쳐야 한다(힘들어서가 아니라 **틀렸다는 증거**가 생긴 것이다).
  ④ **기준2는 오늘 자동 루프의 일이 아니다.** 4점 마커판(T9)·캘리퍼스(T10)·조명(T23)이
  전부 사람 손이고, T20(사람 없이 거울 판정)은 시도했으나 **미결**로 끝났다
  (docs/인수인계-2026-09-04.md §21.5b). 그러니 남은 14일의 자동 몫은
  **기준1·3·4**이고, 기준2는 아침 5분에 걸려 있다.
  ⑤ **작업판이 한 사이클 동안 tester·auditor에게 줄 일이 없었다**(열린 tester 7건이
  전부 사람 손 blocked, auditor 0건). 이건 플래너의 잘못이다 → T36·T37·T38·T39 신설.

- **2026-09-18 (플래너, 사이클 29)** — ① **기준3의 벽이 잡음이 아니라 처짐이라는 것이 실기로 갈렸다.**
  T36(테스터 사이클28이 돌려 03:21에 끝난 원자료 = 젯슨 `~/t36_repeatability.log`, 5왕복)을
  이번 사이클에 계산했다: 같은 관절값으로 돌아왔을 때 TCP 퍼짐이 **A자세 1.51mm ·
  B자세(r237,z423) 1.92mm**다(최대 짝거리). **15mm보다 한 자릿수 작다 → 기준3의 15mm는
  물리적으로 가능하다.** ⚠ 이 반복성은 **인코더 FK 좌표**의 것이다(기준3도 같은 좌표에서
  채점하니 옳은 잣대지만, 감속기 유격·링크 휨은 못 잡는다).
  같은 로그가 더 중요한 숫자를 준다 — **지령↔실제 관절 오차가 A자세 1.1~1.3°, B자세
  4.0~4.9°로 자세마다 거의 일정하다.** T33의 실기 오차 44.6/36.4mm(닿는 표적)는 그러므로
  **되풀이되는 처짐**이고, 되풀이되면 지울 수 있다. → T44(먹는지 잰다) → T45(코드에 넣는다).
  ⚠ 같은 값을 다시 쓰는 것으로는 안 된다(이미 정상상태다) — **오차만큼 더** 줘야 한다.
  토크 포화로 안 지워지면 그때 기준3의 15mm를 다시 본다(**틀렸다는 증거**가 그때 생긴다).
- ② **기준1은 사이클25 이후 '링크 길이는 실측·강제, 마운트 4개는 미실측'이다.** 마운트 TF는
  이제 `bringup_check`가 yaml과 대조해 **틀리면 실패한다**(5mm 돌연변이로 확인). 남은 것은
  숫자를 재는 일(T12, 사람)뿐이다.
- ③ **기준4는 오늘도 충족**: 45 / 90 / 192 / 73 **넷 다 RC=0**(03:2x PC). 400종.
- ④ **사람 아침 5분의 안내를 고쳤다 — D405는 손목에 달려 있다.** '카메라 앞 20~40cm'는
  사람이 방향을 알 수 없어 실행 불가능한 지시였다. 이제 T9는 **조작대 화면
  (http://192.168.0.19:8090/)을 보며 그 화면에 마커판이 들어오게 세우라**고 말한다.
  같은 이유로 `cmean`은 조명만의 숫자가 아니다(03:22 팔=A자세에서 8.6, 00:26 다른 자세에서
  67) — T23의 판정도 **팔을 무대 쪽에 세운 한 자세에서** 재야 뜻이 있다.

- **2026-09-18 (플래너, 사이클 36)** — ① **기준3이 0/5에서 1/5로 바뀌었다 — 자동 루프가 만든 첫 성공이다.**
  사이클35(T47) 실기: 되먹임(T45)이 붙은 뒤 닿는 표적 둘의 오차가 **44.6 → 18.34mm**,
  **36.38 → 10.75mm(성공, 15mm 아래)**로 줄었다. 사이클29에서 "되풀이되는 처짐은 지울 수 있다"고
  적은 짐작이 **실기로 확인됐다**. 남은 셋은 여전히 실패다.
- ② **남은 셋이 실패하는 이유가 갈렸다 — 사거리가 아니라 z다(팔이 그 높이를 못 든다).**
  5회를 어깨 혼 기준으로 다시 계산했다(최대 기구학 사거리 l1+l2+l3=422.5mm):

  | n | 목표 r,z | 어깨기준 거리 | 사거리% | 스탠드오프30 뒤% | 실제 도달 r,z | z 처짐 | 오차 | 판정 |
  |---|---|---|---|---|---|---|---|---|
  | 1 | 321.6, 355.8 | 424.9 | 100.6% | 93.5% | 283.0, 363.4 | −7.6 | 18.34 | 거의 |
  | 2 | 378.8, 264.0 | 435.0 | 103.0% | 95.9% | 345.9, 271.6 | −7.6 | **10.75** | **성공** |
  | 3 | 315.6, 400.3 | 446.4 | 105.7% | 98.6% | 339.0, 285.1 | **+115.2** | 110.14 | 실패 |
  | 4 | 313.8, 372.4 | 428.0 | 101.3% | 94.2% | 337.7, 254.7 | **+117.7** | 121.30 | 실패 |
  | 5 | 231.7, 481.4 | 447.5 | 105.9% | 98.8% | 340.7, 291.1 | **+190.3** | 207.53 | 실패 |

  **사거리%로는 안 갈린다** — 4번은 94.2%인데 실패하고 2번은 95.9%인데 성공했다.
  갈리는 것은 **z**다(성공·거의 z≤363 / 실패 z≥372). 실패 셋은 z를 **115~190mm 떨궜고**,
  도달 z가 255~291mm에 몰린다. 되먹임으로 안 지워지는 종류다(서보 토크의 벽).
  → 경계를 재는 것이 **T40(p1)**, 그 경계 안에서만 표적을 뽑는 것이 **T41**.
- ③ **기준3의 채점 도구에 숨은 수동 선행이 있었다.** 사이클35는 A자세에서 그대로 돌려
  5/5 pose가드 거절(=0/5)을 받고, 사람이 `arm_extend --hold`를 따로 돌린 뒤에야 1/5를 받았다.
  다음 사이클이 그 단계를 잊으면 **거짓 0/5**가 기록된다 → **T49**(builder)가 move5_check에
  자동 뻗기를 넣는다.
- ④ **직교 직선 보간이 이 작업공간에서 사실상 안 쓰인다** — 사이클23·35 기록 10줄 중 8줄이
  "직선 경로가 막혀 관절공간으로 돌아갔다(N걸음 중 **1번째**에서 elbow_flex −102~−124, 한계 ±98)"다.
  끝점 둘 다 IK가 풀리는데 첫 중간점이 늘 막힌다 → **T50**(auditor)이 원인을 가른다.
- ⑤ 기준4는 오늘도 충족: **45 / 102 / 201 / 91 넷 다 RC=0**(04:3x PC, 439종).
- ⑥ 기준1·2는 사이클29에서 바뀐 것이 없다. 기준2는 여전히 **사람 손 넷**(T9·T10·T12·T23)에 전부 걸려 있다.

- **2026-09-18 (플래너, 사이클 43)** — ① **사이클42의 "0/5"는 기준3의 점수가 아니다 — 지우고 읽어라.**
  T54는 `prep`이 실패해 팔이 물리적으로 안 움직인 채(뻗기 전후 관절차 ≤0.6°) 5회를 돌렸다.
  **기준3의 신뢰할 수 있는 최고 기록은 여전히 1/5**(사이클35)이고, 그것조차 사람이
  `arm_extend --hold`를 손으로 돌려 준 판이다. **자동으로 끝까지 도는 판정은 아직 0회다.**
- ② **그 prep 결함의 원인을 이번 사이클에 코드로 다시 잡았다 — 테스터의 진단이 절반만 맞다.**
  "걸음 쪼개기가 없다"가 아니다: `hardware/escape.py:122-126`의 `plan()`은 이미 12° 이하로
  균등히 쪼갠다(18걸음이 그 증거다). 거절한 쪽은 `arm_source.move_joints_deg`
  (`ros2/src/tomato_bridge/tomato_bridge/arm_source.py:160-194)로, **매 호출마다 지금 실제
  자세에서 다시 plan()** 하고 두 걸음 이상이면 거절한다. 그런데 `move5_check.run_prep`
  (`ros2/tools/move5_check.py:433-447`)은 **처음 자세에서 짠 18걸음을 그대로 차례로 보낸다.**
  한 걸음을 보내면 팔은 중력으로 지령보다 **4~5° 못 미친 자리**에 선다(사이클29 T36 실측
  4.0~4.9°). 그러면 2걸음째는 실제 자세 기준 **12+4.3 = 16.3°** — 기록의 그 숫자가 정확히
  이 합이다. 즉 병은 "쪼개는 로직 부재"가 아니라 **쪼갠 걸음이 낡았다**는 것이고,
  고칠 자리는 escape.py가 아니라 **걸음마다 되읽고 다시 짜야 하는 부르는 쪽**이다(T56 note).
  같은 뿌리로 `arm_extend.py`·click-server `park`도 봐야 한다 — **park이 막혔다는 것은
  "팔을 대기 자세로 돌린다"는 표준 마무리가 지금 A자세에서 불가능하다는 뜻**이다.
- ③ **실기 기록이 저장소에 안 남았다 — 사이클42의 5회는 숫자로 다시 읽을 수 없다.**
  `docs/시험기록/move-to-point-2026-09-18.jsonl`은 52줄이고 끝 11줄이 전부 `cycle=35`,
  마지막 커밋은 사이클41(builder)이다. T54의 5회는 **산문에만** 있다. 기록은 다음 사이클이
  사실로 믿는 유일한 것이므로 이것은 도구 결함으로 다룬다 → **T58**.
  같은 줄기로, prep 실패 뒤에도 5회를 돌려 **거짓 0/5**를 남기는 동작도 고친다 → **T59**.
- ④ **기준4는 오늘도 충족**: 45 / 102 / 243 / 91 **넷 다 RC=0**(05:4x PC, 481종).
- ⑤ 기준1·2는 사이클36에서 바뀐 것이 없다. 기준2는 여전히 **사람 손 넷**(T9·T10·T12·T23).
  다섯째였던 T15(거울을 눈으로 확인)는 **버렸다** — 같은 질문을 T9→T13의 잔차가 답하고,
  아침 5분 목록을 넷으로 유지하는 편이 사람에게 낫다.

- **2026-09-18 (플래너, 사이클 50)** — ① **기준3이 3/5다 — 자동 루프가 처음으로 끝까지 돈 판정이다.**
  사이클45(T57)는 인자 없이 `move5_check`를 돌려 `prep.extended=true`(가드 −224 → +237.4mm,
  28걸음)로 스스로 뻗었고, 사람이 `arm_extend`를 손으로 돌려 주지 않았다. 기록은 저장소
  `docs/시험기록/move-to-point-2026-09-18.jsonl` **53~57행**에 실제로 남아 있다.
  사이클43이 "자동으로 끝까지 도는 판정은 아직 0회"라 적은 것은 이 판으로 **해소됐다**.
- ② **남은 2/5가 왜 실패하는지에 대한 사이클36의 답("z가 높아서")은 틀렸다.** 기록의 다섯
  줄을 **설 자리**(스탠드오프 30mm 뒤)로 환산하고 `kin.inverse`로 관절을 역산해 보면:

  | 회 | 설자리 r | 설자리 z | 지령 pan,lift,elbow,wflex | 도달(역산) | **lift 처짐** | 오차mm | 판정 |
  |---|---|---|---|---|---|---|---|
  | 1 | 292.6 | 347.9 | 34.4, 65.4, −30.8, −19.4 | 34.0, 65.9, −30.3, −19.6 | +0.5 | 5.21 | 성공 |
  | 2 | 290.6 | 383.7 | −2.3, 55.0, −21.7, +0.3 | −2.7, 55.8, −20.8, +0.7 | +0.8 | 12.06 | 성공 |
  | 3 | 285.3 | 363.0 | −21.8, 65.3, −27.2, −19.9 | −22.4, 65.7, −26.9, −20.7 | +0.4 | 3.31 | 성공 |
  | 4 | 215.2 | 456.4 | −19.0, 63.8, −22.0, **+14.8** | −19.8, 57.1, −28.0, +11.1 | **−6.7** | 91.16 | 실패 |
  | 5 | 121.7 | 500.1 | 41.3, 78.0, −29.9, **+29.2** | 40.6, 68.4, −35.0, +26.5 | **−9.6** | 102.69 | 실패 |

  **r로는 안 갈린다** — 실패가 오히려 작다(215·122 < 285~293mm). 그래서 지금 유일한 경계인
  `load_limits.r_max=310mm`이 이 둘을 **통과시켰다**. **lift 각도로도 안 갈린다**(성공 1·3이
  65.4·65.3°인데 실패 4가 63.8°다). 갈리는 것은 **처짐**이다 — 성공 셋은 lift 처짐이
  +0.4~+0.8°(사실상 없다), 실패 둘은 −6.7·−9.6°. 그리고 그 둘만 **wrist_flex가 양수**다
  (+14.8·+29.2 대 성공 −19.9~+0.3). ⚠ 처짐은 **표적을 뽑는 시점에 알 수 없는 양**이므로
  그대로는 경계로 못 쓴다 — 자세에서 바로 나오는 양(wrist_flex 부호가 후보다)으로
  번역돼야 한다. 그 판정이 **T62**(auditor)다. ⚠ 도달 관절은 기록의 TCP에서 IK로 역산한
  것이다(기록에 관절 칸이 **없다**) — 그 칸을 넣는 일이 **T64**(builder).
- ③ **표적을 좁혀서 점수를 올리는 것은 금지한다.** 기준3의 "임의의 자리"가 "우리가 갈 수
  있다고 믿는 자리"로 조용히 줄어들면 5/5는 뜻을 잃는다. 그래서 조건을 **하나 더한다**
  (낮추는 게 아니라 **조인다**): 5/5를 졸업으로 인정하려면 그 판의 기록 줄에서
  `load_limits` 칸이 **`none`도 `config.ARM_LOAD_R_MAX`도 아니어야** 한다 — 이 팔에서
  실제로 재어 `~/arm_load_limits.json`에 적은 경계여야 하고, 그 경계가 어디서 왔는지가
  문서에 있어야 한다. (06:47 확인: 젯슨에 `~/arm_load_limits.json`은 **아직 없다** —
  지금까지의 모든 판은 config 기본값으로 돌았다.)
  ⚠ **2026-09-18 사이클65 metrologist 확인**: 사이클64에서 z_max를 추가한 뒤 실제 기록
  태그는 `"config.ARM_LOAD_R_MAX+Z_MAX"`로 바뀌었다(move5_check.py:750 load_tag=load.source).
  **이 조건은 코드로 강제되지 않는다** — move5_check가 config 기본값으로 돌아도 5/5를
  기록하고, 채점자(사람·다음 사이클)가 기록을 보고 판정하는 구조다. 무효화 조건을 강제하려면
  move5_check가 `load.source`에 config 태그가 들어오면 경고를 출력하거나 졸업 판정을
  막는 검사를 추가해야 한다 — builder 작업으로 넘긴다(T71 필요).
- ④ **기준4는 오늘도 충족**: 45 / 102 / 270 / 91 **넷 다 RC=0**(06:4x PC, 508종).
- ⑤ **기준2의 전제가 또 바뀌었다 — 이제 어두워서도, 마커판이 없어서도 아니다. 카메라가
  딴 데를 본다.** 06:47에 젯슨의 `/dev/shm/d405_color.jpg`를 받아 **눈으로 봤다**: 화면에
  들어온 것은 방바닥·전원어댑터·책상 천·플라스틱 서랍장이고 무대도 마커판도 없다.
  밝기는 더 이상 문제가 아니다 — 같은 시각 **cmean 105.3 · cp99 213**(T23 문턱 60/150을
  넘었다), 깊이 유효 25.0% → **82.4%**, 점 검출 0개 → **1개**. 손목 카메라라 **팔 자세가 곧
  겨냥**이고, 사이클45 tester가 뒷정리로 세워 둔 자세가 지금의 겨냥이다. → T23은 사람
  손에서 풀려 **reopen**(남은 몫 = 무대 겨냥 자세에서 재측정 + 이제 방이 밝으니
  자동노출로 되돌리는 편이 나은지 판정), 겨냥을 찾는 일이 **T65**(tester).
  **아침 5분 목록은 넷에서 셋으로 줄었다 — T9·T10·T12.**
- ⑥ 기준1은 사이클43에서 바뀐 것이 없다(링크 길이는 실측·강제 / 마운트 4개는 미실측 = T12).
  사이클49 tester가 `bringup_check.sh` 15/15 RC=0으로 마운트 TF↔yaml 강제를 독립 재확인했다.
- ⑦ **새 안전 결함 하나**: 서비스 전환(`tomato-voice`↔`click-server`)만으로 `shoulder_lift`가
  최대 **77°** 낙하해 TCP가 바닥 아래(−32~−54mm)로 떨어진다(사이클49 실기 3회 재현).
  화분 충돌 위험이라 **T61**을 p1로 둔다 — 졸업 기준에 직접 걸리지는 않지만, 실기를 쓰는
  모든 사이클이 이 위에서 돈다.

- **2026-09-20 (플래너, 사이클 402)** — ① **기준3과 기준2가 여전히 실측 지연으로 묶여 있다.** 
  T10(사람 손목축 실측)에 이어 T9(조작대 각도 조정 후 관절 재수집)가 임계 경로이므로 우선순위를 p1로 올렸다.
  ② 기준4 자체검증은 꾸준히 초록(RC=0) 유지 중.
  ③ 기준3의 5/5 강제 누락(load.source config 태그 거부)과 관절 역산 기록(T64) 등 코드 보완이 필요하나, 현재 p1~p2가 5건으로 한도라 작업 추가는 보류한다.

- **2026-09-20 (플래너, 사이클 411)** — ① **T10 물리 실측 완료 및 작업판 정리.** 사람이 캘리퍼스 실측(-52, 0, 59 mm)을 완료하여 T10을 완료 처리했다(잔차 검증은 T13과 중복이므로 이관). 이제 기준2의 임계 경로는 온전히 젯슨 실기 조작(T9 각도 정렬 → T13 새 표본 채집)이다.
  ② **기준3 무효화 조건 코드화 작업(T75) 추가.** 열린 p1~p2 작업 수에 여유가 생겨, 기준3의 5/5 성공 판정을 강제하기 위한 config 태그 거부 로직 추가를 빌더에게 할당했다.
  ③ 젯슨 실기 접속이 회복되어 테스터가 T9/T13/T65 등 하드웨어 조작을 즉각 수행할 수 있다.

- **2026-09-20 (플래너, 사이클 421)** — ① 기준4 자체검증은 600/601(RC=0) 통과로 합격 유지(CRLF 1건 예외). ② 기준2·3의 실기 측정(T9/T13/T65)은 젯슨이 QEMU 가상 환경에 물리 하드웨어 미연결 상태라 전면 대기(Blocked) 상태다. ③ 중복 발급된 T76 작업은 기존 T71/T75에서 이미 완료되었음을 확인하고 닫았다.

- **2026-09-20 (플래너, 사이클 442)** — ① 기준2·3 실기 측정(T9/T13/T65/T17) 및 선행 의존(T67)을 물리 장치 미연결(T77 대기) 사유로 모두 Block 처리해 상태를 현실과 일치시켰다. ② 기준4는 자체검증 619종 전수 합격(All RC=0)으로 유지 중이다. ③ 사람 손(T12, T77) 대기로 막힌 상태가 지속되어 새 작업을 발급하지 않는다.

- **2026-09-20 (플래너, 사이클 451)** — ① 기준4는 **자체검증 626종 전수 합격(All RC=0)** 으로 상향 유지 중이다(`ros_selfcheck`가 386종으로 증가). ② 기준2·3의 실기 조작은 여전히 젯슨 물리 장치 미연결(T77)로 전면 대기 중이다. ③ 사람 손(T12, T77) 대기 상태가 지속되어 새 작업은 발급하지 않는다.

- **2026-09-20 (플래너, 사이클 471)** — ① 기준4 자체검증이 632종으로 늘었으며 전수 합격(RC=0)을 유지 중이다. ② 기준2·3의 실기 조작은 젯슨 물리 장치 미인식(T77) 지속으로 전면 대기 상태다. ③ 사람 손 대기(T12, T77)로 인해 새 작업은 발급하지 않는다.

- **2026-09-21 (플래너, 사이클 491)** — ① 기준4 자체검증이 650종으로 늘었으며 전수 합격(RC=0)을 유지 중이다 (D405 깊이 거절 명세 추가). ② 기준2·3 실기 조작은 여전히 젯슨 물리 장치 미인식(T77)으로 전면 대기 상태다. ③ T48 역시 물리 장치 구동이 필요하여 Block 처리했다. 사람 손 대기(T12, T77)로 인해 새 작업은 발급하지 않는다.
