# 최상위 목표 (2026-09-17 ~ 10-02, 15일)

## 한 줄
**`v2.0.0-ros.2` 졸업** — 좌표로 보내면 집게가 거기로 간다.

## 합격 기준 (docs/ros2-이행계획.md §3)
1. 링크 길이·마운트를 **자로 잰 실측값**으로 URDF와 `kinematics.py`가 일치한다.
2. 손-눈 보정 **잔차 ≤ 15mm** (`handeye_resolve.py`가 찍는 residual).
3. 임의의 자리에 둔 표적을 `/arm/move_to_point`로 **5회 중 5회** 스탠드오프까지 도달.
4. PC 자체검증 175종 전부 통과 (`ros_selfcheck` 70 · `handeye_check` 45 · `eye_check` 60).
5. 위 넷이 되면 `tools/tag.sh v2.0.0-ros.2 "..."`.

## 지금 알려진 최대 장애물
- **평행이동 오차가 실측의 3~5배** (docs/인수인계-2026-09-04.md, 미해결).
  회전은 브래킷 유격 고정 + 재캘리로 해결됐다.
  ⚠ **그런데 '실측 75~80mm'라는 기준 자체가 의심스럽다**(2026-09-17 플래너).
  `ros2/tools/handeye_from_axes.py`를 젯슨 `~/joint_axes.json`(09-01)에 돌리면
  `t_tool_cam=(-98.2, 33.5, 57.8)mm · |t|=119mm · 거리잔차 7.8mm · 회전 0.68°`가
  나온다. 이행계획 §"camera_at"이 적은 실물 배치(뒤 7~8cm · 위 7~8cm)의 벡터
  크기는 √(80²+80²)≈113mm이므로 **119mm 쪽이 배치와 맞는다.** 09-04 내내 쓴
  `--expect-t-mm 60 100` 게이트가 옳은 해를 버리고 있었을 수 있다 → T8에서 판정.
  (⚠ 이 119mm는 **브래킷 재접착·재교시 전** 데이터다. 재수집 = T10.)
- 남은 수는 **넣을 실측 숫자**이지 코드가 아니다 — §8-1이 "새로 짜야 한다"고 한
  `--fix-t`는 이미 있다(`handeye_resolve.py:334`, 커밋 05f6639). → T9(사람 실측).
- 카메라 링크 관절(롤)을 만지면 `~/grip_uv.json`이 무효가 된다(개발일지-2026-09-11 §1).

## 하지 않을 것
- 기존 스택(`src/tomato_picker/`) 기구학 재작성. 라이브러리로 계속 쓴다.
- ROS를 웹 서비스에 붙이기.
- master에 `-ros.N` 태그, ROS 브랜치에 `v1.x` 태그.

## 갱신 규칙
플래너만 이 파일을 고친다. 고칠 때는 **무엇이 사실로 바뀌어서** 고치는지 한 줄 남긴다.

## 사실이 바뀌어서 고친 기록
- **2026-09-17 (플래너, 사이클 1)** — ① 기준4(175종)는 **오늘 PC에서 충족**한다:
  handeye_check 45/45 · eye_check 60/60 · ros_selfcheck 70/70 · arm_cartesian_check
  33/33. 단 `.venv`에 `pyyaml`이 없어 ros_selfcheck가 죽고 있었고 그걸 통과로
  오독할 뻔했다(T11). ② 기준1의 링크 길이는 2026-08-31에 이미 실측됐고
  `ros_selfcheck`가 yaml↔코드 일치를 **강제한다**(1mm 틀면 실패 확인). 남은 미실측은
  `so101_geometry.yaml`의 `mount.x: 60.0` 하나뿐. ③ 최대 장애물 절에 위 119mm 근거를
  추가했다 — 목표를 낮춘 게 아니라 **기준으로 삼던 75~80mm가 틀렸을 가능성**을 적었다.

