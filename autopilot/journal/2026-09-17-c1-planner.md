# 2026-09-17 사이클 1 · planner

## 큰 그림 (15일 중 1일째)
- 기준 4개 중 **1·4는 사실상 서 있다**(아래 채점). 남은 건 2(손-눈 잔차)와 3(5/5 도달)뿐이고,
  둘은 사실상 같은 병 하나 — **T_tool_cam의 평행이동**에 걸려 있다.
- 09-05 이후 12일간 커밋 20건이 전부 `stem_grasp.py`(화소 시각서보)로 갔다. 그건
  인수인계-2026-09-04 §8-3의 합리적 선택이었지만, **ros.2 트랙은 그동안 멈춰 있었다.**
  졸업하려면 지금 다시 켜야 한다.
- 속도 판단: 막힌 곳이 코드가 아니라 **실측 숫자 3개**라면 14일은 넉넉하다. T9(캘리퍼스
  5분)가 안 풀리면 그때 ros.2를 재정의하는 논의를 해야 한다 — 아직은 아니다.

## 자체검증 기준선 (PC, .venv py3.13.7, 2026-09-17 21:50)
| 스크립트 | 결과 | 종료코드 |
|---|---|---|
| `tools/handeye_check.py` | ✅ 45/45 | 0 |
| `tools/eye_check.py` | ✅ 60/60 | 0 |
| `ros2/tools/ros_selfcheck.py` | ✅ 70/70 | 0 |
| `tools/arm_cartesian_check.py` | ✅ 33/33 | 0 |
| 합계 | **208/208** (졸업기준의 175 = 45+60+70) | |

⚠ **함정(실측)**: 처음 돌렸을 때 `ros_selfcheck`는
`ModuleNotFoundError: No module named 'yaml'`로 죽었다. `.venv`에 pyyaml이 없었다.
`| tail`을 거치면 `$?`가 0이라 **통과로 오독하기 쉽다.** `pip install pyyaml`로 해결. → T11

## 확인한 사실 (명령과 출력)
1. **젯슨은 192.168.0.19** (.8은 옛 IP). `hostname`=localhost.localdomain, up 57분.
   서비스: `click-server`/`depth-cam`/`astra-cam`=active, `tomato-voice`=activating,
   `controller-drive`=inactive. → T2 닫음
2. **URDF↔kinematics 일치 검사는 이미 있다.** `ros_selfcheck.py:303` `test_geometry_matches`,
   `:312` `test_urdf_matches_kinematics`. `so101_geometry.yaml`의 `l2`를 138.0→139.0으로
   틀자 `FAIL l2 일치  yaml 139.0 vs 코드 138.0`, `❌ 1개 실패 / 70개 중`, RC=1.
   되돌려 놨다(git diff 없음). → T4 닫음
3. **`--fix-t`는 이미 있다.** `handeye_resolve.py:334`, 커밋 `05f6639`(09-05).
   인수인계-2026-09-04 §8-1의 "아직 이 모드가 없다 — 새로 짜야 한다"는 **낡았다.**
4. **젯슨 `~/arm_eye.json`은 09-04 00:12자 그대로**: `rms_mm=13.934`, `samples=7`,
   `t=[-33.78, 24.88, -72.62]`. 숫자만 보면 15mm 이하지만 §2가 "5번 더 수집해 단 한 번도
   근처에 못 갔다"고 적은 **재현 안 된 일회성 값**이다.
5. **새 단서 — `handeye_from_axes.py`의 독립 해.** 젯슨 `~/joint_axes.json`(09-01)을
   PC로 받아 돌렸다:
   ```
   python ros2/tools/handeye_from_axes.py joint_axes.json --home="-9.7037,69.3599,7.6907,-83.9883,98.1874"
   [회전] 평균 어긋남 0.68°   (관절별 0.43~0.80°)
   [위치] t_tool_cam = (-98.2, 33.5, 57.8) mm · |t| 119mm · 거리잔차 RMS 7.8mm
   ✅ 축과 거리가 모두 맞는다
   ```
   전역 해가 매번 낸 280~435mm와 다르다. 그리고 이행계획이 적은 실물 배치
   (무는 지점에서 **뒤 7~8cm · 위 7~8cm**)의 벡터 크기는 √(80²+80²)≈**113mm**다.
   → **119mm가 배치와 맞고, 09-04 내내 쓴 `--expect-t-mm 60 100` 게이트는 옳은 해를
   버리고 있었을 수 있다.** (짐작이다. 판정 = T8)
   ⚠ 이 데이터는 브래킷 재접착·재교시 **전**이다 → 재수집 = T10

## 채점 (졸업 기준 5개)
1. **링크 실측 일치 — 거의 됐다.** z0/d0/l1/l2/l3는 08-31 실측이고 yaml↔코드 일치를
   `ros_selfcheck`가 강제한다(위 2번). **안 된 것: `mount.x: 60.0`이 "⚠ 아직 안 쟀다".** → T12
2. **잔차 ≤15mm — 숫자는 있으나 못 믿는다.** 13.934mm가 저장돼 있지만 재현 실패한
   일회성이고, 이후 run은 17.8·20.2mm였다. **안 된 것: 재현 가능한 해.** → T8·T9·T10
3. **5/5 도달 — 모름.** `/arm/move_to_point` 5회 시험을 한 기록이 저장소에 없다.
   근거를 만드는 도구 자체가 아직 없다. → T7
4. **175종 통과 — 됐다.** 208/208(위 표). 단 의존성이 못 박혀 있지 않다. → T11
5. **태그 — 아직.** 1~4가 서기 전엔 달지 않는다.

## 사람이 할 일 (아침에 5분)
- **T9 [사람]** 손목(wrist_roll)축 중심 → D405 렌즈 중심을 **캘리퍼스로 3방향**
  (찌르는 방향 / 그 왼쪽 / 위), mm. l1·l2·l3를 잰 것과 같은 축-축 벡터 관례.
  **이 세 숫자 하나가 기준2·3을 동시에 푼다** — 코드(`--fix-t`)는 이미 준비돼 있다.
- **T12 [사람]** 차체 중심 → 팔 마운트 평면의 앞뒤 거리(`mount.x`). 기준1의 마지막 구멍.

## 작업판 변화
- 닫음: T1(기준선 숫자 확정) · T2(젯슨 .19) · T4(검사 이미 있음, 1mm로 실패 확인)
- 내림: T5 p1→p2 (실행은 T8/T9/T10이 가져감, 문서화만 남음)
- 만듦: T8(게이트 판정) · T9([사람] 캘리퍼스) · T10(joint_axes 재수집) ·
  T11(의존성 못 박기) · T12([사람] mount.x)
- 유지: T3(잔차 강제) · T6(ros.1 표 감사) · T7(5회 시험 도구)
