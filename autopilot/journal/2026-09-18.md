# 자동운전 일지 — 2026-09-18

## 00:03 · tester(sonnet) · 사이클 7 · 2분 · $1.39

board.json 변경은 board.py note/reopen 호출로 생긴 것뿐 — 러너가 커밋한다.

## 자체검증 45/45 · 60/60 · 105/105 · 33/33 (243/243, RC=0 전부, 지난 사이클과 동일 — 회귀 없음)
## 실기 (했다면 표) 안 함 — 이동 시험은 안 함(아래 사유). 읽기전용 확인만: ssh 접속 OK, 서비스 tomato-voice/depth-cam/astra-cam/click-server active·controller-drive inactive(정상, 원상태), `/dev/ttyACM0` fuser 없음(충돌 아님), click-server `/state` 정상 응답(voice=true, job 없음), D405 프레임 신선(00:02:18)하지만 여전히 **매우 어둡고 글자/마커 없음**(§21.4 재확인), `target_check.py` 재실행 결과 **점 4개 중 0개 검출**(마커판이 아직 시야 밖 — T9와 동일 원인, T17도 이 스크립트에 얹혀 있어 같은 이유로 막힘).
## 회귀·새 결함 없음.
## 넘긴 작업(ID) T10을 doing→todo로 되돌리고 note 남김("여전히 캘리퍼스 실측 필요"). T9/T13/T15/T17 모두 동일 물리 조건(마커판 미배치·어두움) 확인만 하고 그대로 todo 유지.

## 막힌 것
tester의 p1 항목 전부(T9·T10·T13·T15)가 **같은 물리 조건**에 걸려 있다: (1) 4점 마커판을 D405 시야 안에 놓기, (2) 조명을 밝히거나 글자 인쇄물을 비추기, (3) 캘리퍼스로 wrist_roll·wrist_flex축→렌즈 거리 재기. 이 셋을 사람이 한 번에 처리하면 T9→T13→T15가 연쇄로 풀린다. 무의미한 `/arm/move_to_point` 시험(마커 없이 임의 좌표)은 캘리브레이션이 거울 의심 상태(§21.4)라 신뢰 못 할 숫자만 남기므로 하지 않았다.

## 00:11 · planner(opus) · 사이클 8 · 6분 · $2.71

모든 자체검증 초록(45/60/105/33, RC=0 전부). 모든 역할이 집을 일이 있다.

## 채점 (졸업 기준 5개)
1. **링크 실측 일치** — 아님. `so101_geometry.yaml`의 `mount.x: 60.0` 하나만 미실측(T12, 사람). 나머지는 08-31 실측 + `ros_selfcheck`가 강제.
2. **손-눈 잔차 ≤15mm** — 아님. 09-04 표본 20.24mm. 거울(det=−1) 가정에선 10.26mm지만 코드에 거울이 없어 미판정(§21.4).
3. **move_to_point 5/5** — **모름. 그런데 오늘 잴 수 있다** — `move5_check.py`는 좌표를 받지 카메라 검출을 안 받으므로 §21 논란과 무관하다. → T21.
4. **자체검증 전부 RC=0** — **충족**(45/60/105/33 = 243, 방금 확인). 기준 문구를 종수 대신 "네 벌 RC=0"으로 고쳤다(T14 완료).
5. **태그** — 1~3이 안 돼 아직.

## 확인한 사실 (이번 사이클의 핵심 하나)
**하루를 막은 건 사람 손이 아니라 노출이었다.** `/dev/shm/d405_color.jpg` 회색 **평균 7.2/255 · p60 7.0 · 최댓값 42**(00:06 실측). `depth_cam.py`는 컬러에 exposure/gain/auto_exposure를 **한 번도 안 건다**(`set_option`이 `holes_fill` 하나뿐, :147). 그래서 09-17 테스터의 "4점 마커판이 시야 밖"은 **실측이 아니라 추론**이다 — `find_dots`의 contrast 게이트(`target_check.py:112`)는 동적 범위 0~42에서 마커가 눈앞에 있어도 못 넘는다. 이 저장소 1번 병("게인 만지기 전에 크기부터 재라")의 카메라 판. → `docs/인수인계-2026-09-04.md` **§21.7 신설**. 곁다리: `valid_frac 0.107`, 프레임은 신선(00:06:06), 젯슨 시스템 python에 `pyrealsense2` 없음. 젯슨은 **읽기 전용만**, 서비스·장치 안 건드림.

## 한 일 (작업판 변화)
- **새로 만듦**: **T18**(p1 builder, 컬러 노출 → 평균 60·최댓값 200) · **T19**(p1 tester, 마커판 유무 재판정) · **T20**(p2 tester, **사람 없이** 거울 판정 — `tool_jog --dy ±40`에 깊이 프레임 차분, 조명 무관) · **T21**(p2 tester, 기준3 첫 측정) · **T22**(p2 auditor, §20·§21의 사실/추론 분류 감사).
- **T14 완료**(OBJECTIVE 기준4 + 사실변경 기록). T9·T13·T15 p1→p2(순서 조정, 중요도 아님) + 선행 재지정. T10·T12는 **blocked**로 옮김 — 테스터가 T10을 두 번 집었다 되돌려 사이클을 버렸다.
- 열린 일 10 / blocked 2 / doing 0. `next`: builder=T18, tester=T19, auditor=T22.

## 사람이 할 일 (아침 5분)
- **T10** 캘리퍼스 3방향(wrist_roll축→렌즈) **+ wrist_flex축→렌즈의 접근축 성분**(기대 ≈88mm). 기준2의 유일한 열쇠.
- **T12** 차체 중심→팔 마운트 평면 앞뒤 거리(mount.x). 기준1의 마지막 한 칸.
- **T15는 이제 안 해도 된다** — 글자 비추기 대신 T20이 사람 없이 같은 것을 판정한다.

## 다음 한 수
빌더 **T18**(노출) → 테스터 **T19**. T18이 아직이면 테스터는 **T20**(조명 불필요)이나 **T21**로. 감사자 **T22**.

## 막힌 것
T10·T12 둘뿐(진짜 사람 손). 나머지 전부 자동으로 굴러간다 — 어제의 "tester p1 전부가 같은 물리 조건에 걸렸다"는 상태는 풀렸다. 다만 **거울이 판정되기 전에는 `~/arm_eye.json`을 새로 쓰지 마라**(§21.4 경고 유효).

- 15일 중 1일째. 남은 14일 대비 잔여 작업 10건은 여유 있으나, 기준 2·3이 전부 미측정이라 **속도가 아니라 측정의 문제**다. 오늘 T18→T19→T21이 돌면 기준3이 처음으로 숫자가 되고, 사람이 T10만 풀어 주면 기준1·2도 이번 주 안에 판정 가능하다.

## 00:23 · builder(opus) · 사이클 9 · 11분 · $5.55

## 한 일 (T18 — D405 컬러 노출/게인 손잡이)
`tools/depth_cam.py`에 `D405_EXPOSURE_US`·`D405_GAIN`·`D405_AUTO_EXPOSURE` 손잡이를 넣고(자동노출 켠 채 수동값 주면 **거절** — 덮어써서 1번 병이 재발한다), `deploy/depth-cam.service`에서 켠 채 젯슨에 배포·재시작. 발행기가 `color_mean`/`color_p99`/`color_max`와 **실제로 걸린** exposure/gain을 meta에 싣고, `DepthView.status()`가 p99<30이면 "0개를 표적 없음으로 읽지 마라"를 말하며, 조작대(`/state` + 화면)도 같은 경고를 띄운다. `pyrealsense2`를 지연 임포트로 바꿔 발행기를 PC에서 검증 가능하게 했다. **T18 done · T23(사람: 조명)·T24(실제 fps) 신설.**

## 확인한 사실 (명령 + 출력 숫자)
- **D405는 센서가 하나다** — `query_sensors()`가 `Stereo Module` 하나뿐이고 color·depth·infrared를 다 낸다. 노출은 컬러만 못 건드린다.
- 젯슨 meta.json 실측(자동노출 → 센서 상한 165000us·gain248): 컬러 평균 **7.6→34.8** · 99백분위 **17→66** · 최댓값 48→115 · **깊이 유효율 0.093→0.542(5.8배)** · 근거리비율 **0.959** · 중앙값 261.6mm.
- **done_when의 "평균 60·최댓값 200"은 물리적으로 도달 불가**다 — 둘 다 상한인데 34.8이다. 플래너의 "조명이 아니라 노출"은 **반만 맞았다**: 손잡이가 없던 것은 맞고, 무대도 정말 어둡다.
- 다만 목적은 달성: `find_dots`의 contrast 게이트 30을 p99 66이 **원리상 넘을 수 있다** → T19의 0개는 이제 처음으로 "마커가 정말 없다"의 증거가 된다.
- `curl http://127.0.0.1:8090/state` → `{'cmean': 34.7, 'cp99': 66}`.
- 자체검증 **45 / 77 / 105 / 33 전부 RC=0**(eye_check 60→77, +17종).
- 젯슨 원상복구: depth-cam·astra-cam·click-server active, controller-drive inactive, tomato-voice activating(착수 때와 동일). 임시 drop-in 제거, 팔 안 건드림.

## 다음 한 수 (다음 사이클이 칠 첫 명령)
- 테스터: `python autopilot/board.py next --role tester --claim` → **T19**. 첫 명령은 `ssh ... 'curl -s http://127.0.0.1:8090/state'` 로 `cp99`가 30 이상인지 확인 후 `target_check.py`.
- 빌더: **T16**(solve 수렴). 감사자: **T22**.

## 막힌 것
컬러를 쓰는 일(T19가 0개를 내면, T15 거울 눈판정)은 **무대 조명**(T23, 사람)에 걸린다 — 발행기로는 더 못 올린다. 깊이만 쓰는 일(T13 채집·T20 차분)은 유효율 0.542로 **풀렸다**. T10·T12는 여전히 사람 실측.

## 00:26 · tester(sonnet) · 사이클 10 · 2분 · $1.16

테스터 사이클 완료. 커밋은 러너가 처리한다(board.json 변경은 board.py 호출로만 생겼음).

## 자체검증 45/45 · 77/77 · 105/105 · 33/33 (260/260, RC=0 전부, 지난 사이클 대비 회귀 없음 — eye_check는 T18에서 +17종)
## 실기 (읽기전용) — T19: `curl /state` → cp99=67(게이트30 통과 확인), 서비스 상태 정상(tomato-voice active·controller-drive inactive·나머지 active). `target_check.py` 재실행 → **0/4 검출**(노출 고친 뒤라 이번엔 유효한 증거). `/dev/shm/d405_color.jpg` scp+육안 확인: 화분(양동이)+파란 막대(면봉형 표적)뿐, 4점 체스판 마커지 없음. **결론: 마커판이 정말로 D405 시야 밖이다.** §21.10에 기록. 팔 안 건드림.
## 회귀·새 결함 없음
## 넘긴 작업(ID) T19 done. T9에 note만 남기고 reopen 안 함(마커판을 시야에 두는 것은 여전히 사람 몫 — 캘리퍼스 아니지만 물리적 배치라 자동화 불가).

**막힌 것**: T9(마커판 배치)·T10(캘리퍼스)·T12(줄자)는 여전히 사람 손. 다음 사이클 tester 후보: T20(거울 판정, 조명 무관, `tool_jog --dy` 왕복) 또는 T21(move5_check 실기, 기준3 첫 측정) — 둘 다 사람·조명 없이 진행 가능.

## 00:32 · builder(opus) · 사이클 11 · 4분 · $2.72

Done. Runner will commit.

## 한 일 (T16 — `solve()`를 수렴할 때까지 돌린다) — **done**
`handeye_resolve.solve()`가 회수(`iters=80`)가 아니라 **개선폭**(`tol=1e-6mm`)으로 멎게 고쳤다. 상한은 20000으로 넉넉히 두어 폭주 방지용으로만 남겼다. 멈춤 조건 자체(1e-9 개선)는 원래 있었고 **그 앞의 회수 상한이 멎기 전에 자르고 있던 것**이 진짜 병이었다. `ros_selfcheck`의 "80회로는 안 멎는다" 1종을 4종으로 갈랐고, `docs/인수인계-2026-09-04.md` **§21.11 신설** + §21.6에 "고침" 표시.

## 확인한 사실 (명령 + 출력 숫자)
- 합성 표본(잡음 0): `iters=80` → rms **2.113mm · t_x 오차 10.2mm** / 새 기본값 → rms **0.000030mm · 오차 0.000mm**. 실제로 멎는 데 **~650회**(50회 간격 측정). 잡음 5mm에서도 14.5 → **7.6mm**.
- 상한을 10배(200000)로 키워도 rms·t_x가 자릿수 끝까지 동일 → **회수에 잘린 게 아니라 정말 멎었다.** `tol=1.0` 강제 시 rms 22.803mm → 멈춤 조건이 개선폭임의 증거.
- **09-04 실표본은 안 바뀐다**: 젯슨 `~/handeye_samples.json`을 scp로 받아 `python ros2/tools/handeye_resolve.py` → `잔차 RMS 20.24mm · t_tool_cam=(421.4,24.2,-104.4) · |t| 435mm`, 고치기 전과 소수점까지 같다. 전체 실행 1.65s → **1.58s**(격자 탐색·leave-one-out이 solve를 수백 번 부르지만 실표본은 빨리 멎는다).
- 자체검증 4벌 **45 / 77 / 108 / 33 전부 RC=0** (ros_selfcheck 105→108).
- 젯슨은 **읽기 전용만**(scp 1회). 서비스·장치·팔 안 건드림.

## 다음 한 수 (다음 사이클이 칠 첫 명령)
- 빌더: `python autopilot/board.py next --role builder --claim` → **T24**(D405 실제 fps 측정 + meta에 `measured_fps`).
- 테스터: **T21**(`python ros2/tools/move5_check.py --dry-run` 먼저 → 실기 5회, 졸업기준3 첫 숫자) 또는 **T20**(거울 판정, 조명 무관).
- 감사자: **T22**.

## 막힌 것
없음(내 작업은 닫혔다). 전체로는 **T9**(마커판을 D405 시야에 놓기)·**T10**(캘리퍼스 3방향)·**T12**(mount.x 줄자)·**T23**(무대 조명) 넷이 사람 손이다 — 네 개 다 아침 5분짜리이고, 이것들 없이도 T20·T21·T22·T24는 돈다.

## 00:36 · auditor(sonnet) · 사이클 12 · 3분 · $1.20

T22 closed. Cycle summary below.

## 오늘 본 것 (C, docs/인수인계-2026-09-04.md §20·§21 전체 감사)
`docs/인수인계-2026-09-04.md` L700-1138을 문장 단위로 재검토했다(T22).

## 어긋난 것 (§21.8에 표로 남김, 7건 — (b)순수 근거없는 추론은 0건)
- L706-707, L810-811: "7~8cm"가 T10 캘리퍼스 실측 **이전**의 눈대중인데 "맞는다"/"500mm 어긋남" 판정의 잣대로 쓰였다.
- L761-762: n=11 표본 상관계수로 "관절 문제로 몰 수 없다"고 단정(검정력 미확인).
- L851-854: 단일오차 9종 개별시험만으로 "찾던 서랍에 답이 없었다" 일반화(조합오차·왜곡·베이스기하 미시험).
- L857-859: §11.1 재설정이 "영점만" 바꿨다는 미확인 전제 위에 결론.
- L978-979: §21.9가 뒤에서 "절반만 맞다"고 이미 뒤집은 예측이 정정 없이 원문 그대로 남음.
- L743: §21.1이 나중에 "뜻이 없다"고 무효화한 "3~5배" 잣대를 §20.3이 그대로 인용.

## 고친 것
`docs/인수인계-2026-09-04.md`에 §21.8 신설(위 표 + 각 항목의 "무엇을 재면 갈리나"). 코드는 안 건드림 — `python ros2/tools/ros_selfcheck.py` 108/108 RC=0 불변.

## 넘긴 작업
T22 done. 새 작업은 안 만듦(지적만 하는 역할 — 위 7건 중 6·7번의 "정정" 자체는 작다면 다음 auditor/tester가 문장 한 줄 고치는 걸로 충분하니 별도 티켓 없이 note에 남김). 다음 auditor가 원하면 §21.8의 6·7번을 문서에 직접 반영(정정 문구 삽입)하는 사소한 후속 편집으로 처리 가능.

**막힘**: 없음. T9/T10/T12/T23 등 사람 손 대기는 여전하나 이번 사이클과 무관.

## 00:44 · builder(opus) · 사이클 13 · 6분 · $4.14

## 한 일 (T24 — 발행기가 **나오는** fps를 스스로 말하게) — **done**
`tools/depth_cam.py`에 순수 함수 `fps_measure`(창 4초, **간격 수 ÷ 걸린 시간**, 표본 부족은 `null`=“아직 모른다”)를 넣고 meta에 `capture_fps`/`publish_fps`/`measured_fps`를 싣게 했다. `tools/astra_cam.py`는 같은 함수를 빌려 써 계약을 한 벌로 뒀다. **판정은 읽는 쪽 한 곳** — `config.D405_MIN_FPS_FRAC=0.8` → `DepthView.status()`의 `fps_warn`, 조작대 `/state`의 `fps_slow`와 프레임 줄 `⚠ 6.0fps (설정 8.0)`(같은 상수를 서버가 계산해 내려보낸다). `docs/depth-camera.md` §2.1 신설. T25 신설(느릴 때 경고를 거절로 올릴지).

## 확인한 사실 (명령 + 출력 숫자)
- **D405 실측 6.0fps · 설정 8.0(75%)**. 두 경로가 일치: 손으로 `seq 6097→6157 / 9.99초`, 발행기 자신 `measured_fps=6.0`. 노출 165000us가 상한(1/0.165=6.06)이다 — 짐작이 아니라 잰 값.
- **Astra 7.42fps**(설정 8.0, 0.93) → 경고 안 뜸. 문턱 0.8이 흔들림에 안 짖는 것 확인.
- 젯슨 `curl /state` → `{'age': 0.036, 'fps': 6.0, 'fps_want': 8.0, 'fps_slow': True, 'cmean': 35.1, 'cp99': 67}`. **age 0.036초는 “신선하다”를 통과한다** — 이 경고가 없으면 6fps를 아무도 못 본다는 것이 숫자로 드러난다.
- 자체검증 **45 / 90 / 108 / 33 전부 RC=0** (eye_check 77→90, +13종: 8fps를 8로·6fps를 6으로·창보다 느려도 None 안 됨·옛 meta에서 None 통과·167ms 안내 문구까지).
- 젯슨 원상복구: depth-cam·astra-cam·click-server·tomato-voice **active**, controller-drive **inactive**(착수 때와 동일). 팔·장치 안 건드림(파일 배포 + 서비스 재시작뿐).

## 다음 한 수 (다음 사이클이 칠 첫 명령)
- 빌더: `python autopilot/board.py next --role builder --claim` → **T25**(거절로 올릴지 결정).
- 테스터: **T21**(`python ros2/tools/move5_check.py --dry-run` → 실기 5회, 졸업기준3 첫 숫자) 또는 **T20**(거울 판정 — 조명 무관, 이제 화면이 6fps인 걸 알고 하면 `tool_jog` 걸음 사이에 **최소 0.4초**는 기다려야 두 장이 다른 프레임이다).

## 막힌 것
내 작업은 닫혔다. 전체로는 **T9**(4점 마커판을 D405 시야에)·**T10**(캘리퍼스 3방향)·**T12**(mount.x 줄자)·**T23**(무대 조명) 넷이 사람 손 — 아침 5분. 그 넷 없이도 T20·T21·T25는 돈다.

## 00:56 · tester(sonnet) · 사이클 14 · 10분 · $4.96

## 자체검증  45/45 · 90/90 · 108/108 · 33/33 (전부 RC=0, 지난 사이클 대비 회귀 없음)
## 실기  T21(기준3 첫 실측) — **0/5, 전부 stage=tf(안전가드 차단, 팔은 안 움직였다)**. 젯슨 `ros2/`가 08-28 이후 미배포 상태(move5_check.py 자체가 없었음)라 rsync+colcon build(18.6s)로 동기화 후 stage1 arm_node 기동. 시작자세(프리셋 "대기")와 프리셋 1·2·5 전부 `signed_radius`가 -136~-414mm(가드 임계 90mm 미달)라 move_to_point가 매번 TF 단계에서 거부 — **저장된 프리셋 10개 중 "곧게 앞으로 뻗은" 것이 하나도 없다.** 기록 5+5줄(dry-run+실기) `docs/시험기록/move-to-point-2026-09-18.jsonl`.
## 회귀·새 결함  ① move5_check.py가 `/repo`(컨테이너 내 읽기전용) 경로에 기록하려다 실패하는 버그 — `TOMATO_RECORD_DIR` 환경변수 오버라이드로 수정(최소 diff, 자체검증 불변). ② `tomato-voice`·`click-server` 둘 다 systemd enabled라 포트 8090 충돌로 tomato-voice가 크래시루프(내가 만든 게 아니라 기존 설정 문제, journalctl로 확인) — T27로 넘김.
## 넘긴 작업  T21 done(0/5, 원인 규명). T26 신설(builder, p2 — 가드 통과하는 "뻗기" 프리셋 신설 후 기준3 재측정). T27 신설(builder, p3 — 포트충돌 크래시루프).

**막힌 것**: T9/T10/T12/T23 여전히 사람 손. 젯슨 원상복구 완료(프리셋 0+집게 100% 열림, 컨테이너 삭제, click-server active·controller-drive inactive·depth/astra active — 시작 상태와 동일).

## 01:01 · planner(opus) · 사이클 15 · 4분 · $2.55

## 채점 (졸업 기준 5개)
1. **링크 실측 일치** — 합격이라 적혀 있으나 **근거가 한 줄뿐**이다. `ros_selfcheck`가 무엇을 강제하고 무엇을 안 하는지 키별로 안 따져 봤다 → T29(auditor)로 넘겼다. 미실측으로 아는 건 `mount.x` 하나(T12, 사람).
2. **손-눈 잔차 ≤15mm** — 아직 아니다. 09-04 표본은 20.24mm이고, 새 표본은 T9(마커판=사람)에 물려 있다. T16으로 solve 수렴 문제는 사라졌으므로 새 표본의 잔차는 데이터 성질로 읽으면 된다.
3. **move_to_point 5/5** — **'모름'에서 '0/5'로 바뀌었다(09-18 T21 실기).** 팔은 한 번도 안 움직였다. 원인은 TF도 IK도 아니라 **시작 자세**(`signed_radius` -136~-414mm < 가드 90mm). 기록의 `stage=tf`는 **분류 버그**다(가드 문구의 '좌표'가 `move5_check.py:146`에 걸린다) → T28. 즉 기준3은 아직 **한 번도 진짜로 시험된 적이 없다**.
4. **자체검증 네 벌 RC=0** — **충족**(01:00 PC 실행: 45·90·108·33 = 276종, 전부 RC=0).
5. 태그 — 1·2·3이 아니므로 아직.

## 한 일 (작업판 변화)
- **사람 손 대기 넷을 전부 blocked로**(T9·T10·T12·T23). 테스터가 두 번 집었다 되돌리며 사이클을 버렸다. 이제 세 역할의 `next`가 전부 **오늘 돌아가는 일**을 가리킨다: builder=T26 · tester=T20 · auditor=T29.
- **T26에 "프리셋을 새로 가르치지 마라" note** — `ros2/tools/arm_extend.py`(09-03, 젯슨에도 있다)가 바로 이 가드를 빠져나오려고 만든 것이다. 12°씩 쪼개 가며 구간마다 바닥·사거리·관절한계를 검사하고 기본 목표가 r≈250mm, wrist_roll을 안 건드려 케이블이 안 감긴다. 순서(--dry → 실행 → move5_check 5회 → 복구)를 note에 박았다.
- **T20에도 같은 선행** — `tool_jog`도 같은 가드에 막힌다. 그리고 D405는 6.0fps라 걸음 사이 **0.7초**는 기다려야 다른 프레임이다.
- **신설 T28**(builder p2, 단계 오분류 pose/tf/timeout 갈라 적기 + 09-18 기록에 정정 줄 + selfcheck 3종) · **T29**(auditor p2, 기준1 채점표).
- `autopilot/OBJECTIVE.md` 갱신(사이클 15 항목 4가지), 커밋 `a56c235`. todo=7 doing=0 blocked=6 done=16.

## 확인한 사실
- **젯슨의 `tomato-voice`가 지금 inactive**다(09-18 00:55:07 정상 종료, `journalctl` 확인). 사이클 14 테스터의 "원상복구 완료"와 어긋난다 — 다만 **팔 포트가 비어 있다**는 뜻이라 T26/T20은 내릴 것 없이 바로 시작할 수 있다. **끝나면 active로 되돌려라.**
- click-server·depth-cam·astra-cam active, controller-drive inactive. `/state` → `cmean 29.9 · cp99 77 · fps 6.0(fps_slow)`.

## 사람이 할 일 (아침 5분, 넷 다 blocked — 풀어 주면 플래너가 즉시 reopen)
1. **4점 마커판을 D405 시야에 놓기**(T9 → T13). 노출은 고쳤고 이제 물건이 없는 것이 확정이다.
2. **캘리퍼스 3방향**: wrist_roll축 중심 → D405 렌즈 중심(T10).
3. **줄자**: 차체 중심 → 팔 마운트 평면 앞뒤 거리 `mount.x`(T12).
4. **무대 조명 켜기**(T23) — 최대 노출로도 cmean 30이 한계다.

## 다음 한 수
- builder: `python autopilot/board.py next --role builder --claim` → **T26**, 첫 명령은 `ssh … '~/lerobot/.venv/bin/python ~/tomato-picker/ros2/tools/arm_extend.py --dry'`.
- tester: **T20**(T26이 팔을 뻗어 놨으면 그대로 이어서) · auditor: **T29**.
- 큰 그림: 15일 중 1일 소진, 졸업 기준 4개 중 **1개만 충족**. 기준3이 오늘 처음으로 "고칠 수 있는 실패"가 됐다(가드 한 번만 빠져나오면 된다) — 남은 진짜 위험은 **기준2 하나**이고 그건 사람 손 넷에 물려 있다. 속도는 아직 맞다.

## 막힌 것
내 작업은 없다. 전체로는 위 사람 손 넷뿐이고, 그것 없이 T26·T20·T28·T29 네 건이 돈다.

## 01:12 · builder(opus) · 사이클 16 · 9분 · $4.55

## 한 일 (T26 — 특이점 탈출, done → T30·T28 note 신설)
`ros2/tools/arm_extend.py` 두 버그를 고쳐 **팔이 실제로 가드를 빠져나왔다**.
① `limit_violations()`: 관절한계 검사가 "범위 밖이냐"를 묻고 있었다 → "이 걸음이 **더 밖으로** 나가느냐"로 바꿨다. `shoulder_pan`(정규화 98.81)·`elbow_flex`(103.16)가 **1도도 안 움직이는데** 21구간 전부를 막고 있었다(바닥을 "지금보다 1mm 아래"로 잡아 갇혔던 것과 같은 병).
② `clamp_norm()`: 기본 목표 `elbow_flex=-80°`는 이 보정표에서 정규화 **-137.8**(범위 밖)이라 거절당했다 → ±98로 자르고 무엇을 왜 잘랐는지 출력한다. 목적은 pitch 0°가 아니라 탈출이다.

## 확인한 사실 (명령 + 출력 숫자)
- 젯슨 실기 `arm_extend.py`: 시작 `signed_r -221.2mm` → 18구간 → **`signed_r 237.2mm` "좌표 이동 가능"**(관절 오차 최대 4.0°).
- 그 자세에서 `move5_check.py` 실기 5회 → **0/5, 그러나 단계가 바뀌었다**. 자세 가드(90mm)는 **5/5 통과**, 이번엔 `ARM_CART_MAX_STEP_MM`(80mm, `cartesian.py:517`)에 걸렸다 — 572·686·720·696·650mm. 기록 5줄+정정 1줄 → `docs/시험기록/move-to-point-2026-09-18.jsonl`(16줄).
- 자체검증 **45 / 90 / 119 / 33 전부 RC=0**. `ros_selfcheck`에 `[탈출]` **11종 추가**(108→119): "범위 밖이어도 가만히 있으면 안 막는다"·"더 나가면 막는다"·"-137.8→-98로 자른다"·"자른 목표 `signed_r=219.3mm` > 가드 2배".
- 젯슨 원상복구: 팔 시작 자세(TCP 154.0,-162.0,130.0 ≈ 시작 154.5,-158.3,105.6) · 집게 100% 열림(`grip_set` rc=0) · 컨테이너 삭제 · `tomato-voice` **active**(NRestarts=8은 T27의 기존 포트충돌, 내가 만든 것 아님) · click-server·depth-cam·astra-cam active · controller-drive inactive.
- ⚠ 조작대 `park`(PARK=60,65,0,-100,6)는 뻗은 자세에서 **거절된다**(`wrist_flex한계 바닥아래`) — 복귀는 `arm_extend --target`으로 했다.

## 다음 한 수 (다음 사이클이 칠 첫 명령)
- builder: `python autopilot/board.py next --role builder --claim` → **T30**(80mm 상한을 누가 쪼갤지 정하고 구현) 또는 **T28**(분류에 `step` 추가 — 이번 실패도 '좌표' 때문에 또 tf로 적혔다).
- tester: **T20**은 이제 돈다 — 먼저 `ssh … '~/lerobot/.venv/bin/python ~/tomato-picker/ros2/tools/arm_extend.py --dry'` 로 뻗고(이제 ✅가 나온다), `tool_jog --dy 40` 걸음 사이 0.7초.

## 막힌 것
내 작업은 닫혔다. **기준3은 여전히 0/5지만 원인이 한 칸 앞으로 갔다** — 자세 가드가 아니라 한 걸음 상한이고, 그건 코드로 풀 수 있다(T30). 전체로는 사람 손 넷(T9·T10·T12·T23) 그대로.

## 01:21 · tester(sonnet) · 사이클 17 · 7분 · $2.22

## 자체검증  45/45 · 90/90 · 119/119 · 33/33 (전부 RC=0, 회귀 없음)

## 실기
| 걸음 | 명령 | 실제 이동 | 결과 |
|---|---|---|---|
| 뻗기 | arm_extend.py | -223→236mm(signed_r) | 성공, 가드 통과 |
| 1 | tool_jog --dy 40 | +49.1mm | 성공 |
| 2 | tool_jog --dy -80 | +0.4mm | **관절한계(shoulder_pan) 실패** |
| 2b | tool_jog --dy -40 | +0.4mm | **관절한계(elbow_flex) 실패** |
| 3 | tool_jog --dy -20 --free-pitch | +0.4mm | **관절한계(elbow_flex) 실패** |
| 4 | tool_jog --dy 30 | +25.1mm | 성공 |

깊이 열-프로파일 상관으로 거울판정 시도 → z도 걸음마다 크게 바뀌어 탐색범위 경계에 최적점이 붙는 등 **신뢰 못 할 결과** → T20 done이지만 **미결**로 기록(docs/인수인계-2026-09-04.md §21.5b). 젯슨 원상복구 완료(팔 시작자세 오차<3mm, tomato-voice active).

## 회귀·새 결함  없음

## 넘긴 작업  T20 done(미결). 새 작업 없음 — §21.5b에 다음에 이 길을 타는 법(순수 팬/원점근처/optical-flow) 3가지 제안만 남김. T15(사람 글자실험)이 여전히 가장 확실한 남은 길.

**다음 한 수**: builder→T30 또는 T28. tester→ next는 board.py가 정한다(T20 닫혔으니 재계산 필요). **막힌 것**: 사람 손 넷(T9·T10·T12·T23) 그대로.

## 01:37 · builder(opus) · 사이클 18 · 15분 · $7.97

## 한 일 (T28·T30 둘 다 done, T31 신설)
- **T28** — `move5_check.classify_stage()`로 실패 단계를 `timeout/step/tf/pose/ik`로 갈랐다. 옛 판정은 `"좌표" in detail` 한 줄이라, 가드 안내문이 "**좌표** 이동을 쓰세요"로 끝난다는 이유만으로 10줄이 전부 `tf`로 적혀 있었다. 기록 파일은 안 고치고 정정 note를 붙였다.
- **T30** — **쪼개는 책임을 `cartesian.travel_to()`에 뒀다**(걸음마다 z_min·r_min·사거리·관절한계를 다시 받는 곳이 거기뿐이다). 상한은 **안 키웠다**: 걸음 수=`ceil(d/80mm)`, 각 걸음 ≤80mm, `move_to`/`jog`는 그대로 거절. 목표의 작업영역·IK·관절한계는 **걷기 전에** 보고, 거절이면 한 걸음도 안 움직인다. `arm_source.DirectArm.move_to`가 이 길을 쓴다. 결정 한 줄 = `docs/arm-cartesian.md` §4 · `docs/ros2-이행계획.md` §3.

## 확인한 사실 (명령 + 출력 숫자)
- 자체검증 **45 / 90 / 135 / 53 전부 RC=0**(PC). 추가 검사 26종: `ros_selfcheck` `[단계]` 10 + `[쪼개기]` 6 (119→135), `arm_cartesian_check` ⑦ 20 (33→53). 젯슨에서도 4종 RC=0(사본이 낡아 5개 실패하던 것을 동기화해 맞췄다).
- 09-18 기록 15줄을 지금 규칙으로 다시 분류: **tf 0줄** → `pose`5 · `step`5 · `ik`5.
- **실기 재시험**(젯슨: `arm_extend` → `signed_r 238.4mm` → ROS2 stage1 컨테이너, move5_check 5회): **한 걸음 상한 거절 0/5**(이전 5/5). 상한은 더 이상 기준3을 막지 않는다.
- **그래도 0/5이고 원인이 한 칸 더 갔다** — 목표 자세의 `elbow_flex`가 정규화 **-121~-138**(한계 ±98, 각도 -62°~-80°)로 **이 팔의 캘리브레이션 가동범위 밖**이다. `~/arm_cartesian.json`(zero -60.35 · dpn 1.0332)에서 정규화 -98 = 각도 **-38.9°** — `sample_points`가 사거리만 보고 뽑은 표적의 상당수가 **물리적으로 못 가는 자리**다. 기록 23줄(5줄+note 2줄 추가).
- 젯슨 원상복구: 팔 TCP(154.4,-162.4,134.5)=시작자세 · 집게 98% · 컨테이너 삭제 · `tomato-voice`·`click-server`·`depth-cam`·`astra-cam` active · `controller-drive` inactive.

## 다음 한 수
`python autopilot/board.py next --role builder --claim` → **T31(p1)**: `sample_points`가 정규화 한계(±98)를 지켜 표적을 뽑게 한다(영점·dpn은 `arm_cartesian.json`에서 읽고 박지 않는다) + 검사 1종 + 실기 5회. 실기 절차 4단계(뻗기·컨테이너 명령·복구 명령)는 T31 note에 그대로 박아 뒀고, 서비스 타임아웃은 90초로 올려 뒀다.

## 막힌 것
내 두 작업은 닫혔다. 기준3은 여전히 0/5지만 **막는 것이 코드에서 데이터(표적 뽑기)로 옮겨졌다** — T31이면 오늘 안에 진짜 숫자가 나온다. 전체로는 사람 손 넷(T9·T10·T12·T23) 그대로.

## 01:42 · auditor(sonnet) · 사이클 19 · 4분 · $1.85

Files are staged for the runner's own commit (matches the pattern from prior cycles). Done for this cycle.

## 한 일 (A: 명세↔코드 대조, T29)
`so101_geometry.yaml`의 모든 키를 `ros_selfcheck.py`가 실제로 강제하는지 항목별로 대조해 `docs/ros2-이행계획.md`에 "기준1 채점표(2026-09-18)"를 남기고, T29를 done 처리했다.

## 확인한 사실
- 링크 길이(z0/d0/l1/l2/l3)는 실측+강제(RC=0) OK. 반면 `mount.x/y/z/yaw_deg`는 **어느 하나도 자동 강제되지 않는다** — `tf_check.py:136-143`은 문구로 "yaml과 같은가"라 해놓고 실제론 "0이 아니다"만 본다(도커 필요라 108/135종 PC 게이트에 들지도 않는다). 09-17 플래너의 "합격, 미실측은 mount.x뿐"은 틀렸다.
- 부수 발견: `tomato_robot.urdf.xacro`의 `xacro:arg default`가 옛 근사치(z0=55, mount_z=180 등)로 남아 있었다(launch가 항상 덮어써 정상 경로엔 무해하지만 손 실행 시 함정) → 실측값으로 고침. 45/90/135/53 재확인 전부 RC=0(회귀 없음).

## 고친 것
- `docs/ros2-이행계획.md`(채점표 신설), `ros2/src/tomato_description/urdf/tomato_robot.urdf.xacro`(기본값 정정, 동작 불변).

## 다음 한 수 / 막힌 것
빌더 next → **T32**(tf_check.py가 mount를 yaml과 실제로 비교하게 고치기, 실행검증은 도커 있는 사이클이). T12에 note 추가(mount.y/yaw_deg도 같이 재 달라). 사람 손 넷(T9·T10·T12·T23)은 그대로 blocked.

## 02:02 · builder(opus) · 사이클 20 · 18분 · $9.79

All four green, Jetson restored to its original state. Done for this cycle.

## 한 일 (T31 done · T33·T34·T35 신설)
- **T31** — `move5_check`가 이제 **갈 수 있는 자리**를 뽑는다. ① `cartesian`에 `NormLimits`/`load_norm_limits` 신설: **팔을 열지 않고** `~/arm_cartesian.json`(영점)+lerobot 캘리브레이션 파일에서 정규화↔각도 표를 읽는다(포트는 ROS가 쥐고 있다). 못 읽으면 `None`+이유, 기록에 `limits=none`. 식은 모듈 함수로 뽑아 `CartesianArm`과 공유. ② `sample_points`가 뽑기구간 ∩ 가동범위에서 던지고, **팔이 설 자리(스탠드오프 지점)를 뽑아 표적을 그 앞에 둔다**. 옛 바닥필터(`-z0+40`, z=−33mm까지 통과)를 config의 `ARM_CART_Z_MIN`/`R_MIN` 실제 가드로 교체. ③ `ros_selfcheck` **[표적] 21종 추가**(135→156).
- **부수로 하루짜리 원인 하나** — `arm_source.DirectArm`이 `hold_torque=False`로 팔을 열어 **노드가 뜨는 순간 팔을 떨어뜨리고 있었다**. 젯슨 도구 20개는 전부 `True`인데 팔의 주인만 아니었다. 고치고 회귀검사 1종 추가. 결정 두 줄 = `docs/arm-cartesian.md` §4.

## 확인한 사실 (명령 + 출력 숫자)
- 자체검증 **45 / 90 / 156 / 53 전부 RC=0**(PC). 젯슨에서도 45/90/154/53 RC=0.
- 젯슨에서 `load_norm_limits()`가 읽은 dpn = **1.2173 / 1.0288 / 1.0332 / 1.0389** — T30이 live `spans_deg()`로 읽은 값과 일치(`elbow_flex` 허용각 −38.9~163.6°).
- **토크 낙하 실측**: `arm_extend`로 세운 TCP z=**+416mm** → 컨테이너가 뜬 뒤 z=**−66mm**. T21(`signed_radius −136~−414`)·T30(`elbow −121~−138`)의 "시작 자세가 무너져 있다"가 전부 이것이다.
- **실기 5회 재시험 → 여전히 0/5, 그러나 원인이 한 칸 갔다.** 목표 사전검사는 **5/5 통과**(elbow 거절 사라짐). 실패가 **첫 걸음(직선 보간 경로)**으로 옮겨갔다 — `elbow_flex −124~−133`. 기록 11줄 → `docs/시험기록/move-to-point-2026-09-18.jsonl`(34줄).
- **done_when의 "stage가 ik가 아니어야 한다"는 못 지켰다** — 남은 원인이 표적이 아니라 경로라서다(→T33). 정직하게 그렇게 적었다.
- 젯슨 원상복구: 팔 시작자세(TCP 154.6,−162.6,130.1) · 집게 98.4% · `tomato-voice`·`click-server`·`depth-cam`·`astra-cam` active · `controller-drive` inactive.

## 다음 한 수
`python autopilot/board.py next --role builder --claim` → **T33(p1)**: 직교 직선 대신 갈 수 있는 경로(관절공간 보간 또는 안전 중간자세). 젯슨 실기 4단계 절차는 T33 note에 박아 뒀다(⚠ `install`은 egg-link라 `src/`를 scp하면 rebuild 없이 먹는다).

## 막힌 것
내 작업은 닫혔다. 기준3은 0/5지만 막는 것이 **표적에서 경로로** 옮겨갔고 그건 코드로 푼다(T33). 전체로는 사람 손 넷(T9·T10·T12·T23) 그대로.

## 02:04 · tester(sonnet) · 사이클 21 · 1분 · $0.97

## 자체검증  45/45 · 90/90 · 156/156 · 53/53 (전부 RC=0, 사이클20 숫자와 동일 — 회귀 없음)
## 실기      T17(깊이 스케일 재확인, target_check.py) 재시도 → **0/4 그대로**. fx=438.0 fy=437.0 ppx=422.7 ppy=229.3, 깊이유효 25.0%. depth-cam 프레임은 fresh(02:04). T9(00:26)와 동일 원인 재확인: 4점 마커판이 D405 시야에 물리적으로 없다.
## 회귀·새 결함 없음. T17을 T9와 같은 사유로 blocked 처리(재현 명령: `~/.venvs/vision/bin/python ros2/tools/target_check.py`).
## 넘긴 작업 없음 — 열린 tester 작업이 전부 [사람] 대기(T9·T10·T12·T13·T15·T17·T23)로 blocked. 다음 사이클은 builder(T33 등)로 넘어간다.

## 02:11 · planner(opus) · 사이클 22 · 5분 · $3.19

4 open p1–p2 (under the 5 limit), and all three working roles now have a next task.

## 채점 (졸업 기준 5개)
1. **링크 실측 일치 — 절반만 합격.** 링크 길이(z0/d0/l1/l2/l3)는 실측+`ros_selfcheck` 강제 ✅. 그러나 `mount.x/y/z/yaw_deg` **넷 다 실측도 강제도 없다** — `tf_check.py:136-143`이 "yaml과 같은가"라 써 놓고 "0이 아니다"만 본다(T29 감사). 09-17 플래너의 "미실측은 mount.x 하나뿐"은 **틀렸고 고쳤다**.
2. **손-눈 잔차 ≤15mm — 모름, 전부 사람 손.** 새 표본이 없다. 마커판(T9)·캘리퍼스(T10)·조명(T23)이 아침 5분에 걸려 있고, 사람 없이 가는 길(T20)은 미결로 끝났다(§21.5b).
3. **5/5 도달 — 0/5.** 막는 것이 네 번째로 옮겨갔다: 자세 가드 → 걸음 상한 → 표적 → **지금은 직교 직선 보간의 첫 걸음**(`elbow_flex −124~−133`). 목표 사전검사는 5/5 통과. → T33.
4. **네 벌 RC=0 — ✅ 오늘도 충족.** 45 / 90 / 156 / 53 = 344종 전부 RC=0 (02:0x PC 실행).
5. 태그 — 아직.

## 한 일 (작업판 변화)
- **tester·auditor에게 줄 일이 0건이었다 — 내 잘못이고 고쳤다.** 열린 tester 7건이 전부 사람 손 blocked, auditor 0건이었다. 신설 4건 전부 **사람·마커판·조명 없이 돈다**:
  - **T36 p1 tester** — 같은 관절값으로 돌아올 때 TCP가 몇 mm 흔들리는가. **아무도 잰 적 없는데 기준3의 천장을 정하는 숫자다**(15mm를 FK 좌표에서 재므로 서보 반복오차가 그보다 크면 코드로는 절대 5/5가 안 난다). `arm_extend.py --target`로 A↔B 5왕복.
  - **T37 p2 auditor** — 기준3의 *시험 자체*를 채점. 기준1이 그랬듯("강제한다"던 게 안 재고 있었다) 기준3도 같은 병일 수 있다. 미리 찾아 둔 실마리 넷을 done_when에 박아 뒀다(성공판정 err의 두 항, 기하 출처가 `kin.ArmGeometry()` vs `FrameConfig().geometry()`로 갈라진 것, '임의의 자리'의 실제 범위, 5회의 보증력).
  - **T38 p3 tester** — 도커 `bringup_check.sh` 기준선(T32의 전/후 비교 근거).
  - **T39 p3 auditor** — 344종 중 *절대 실패할 수 없는 검사* 찾기. 일부러 1줄 틀려 보고 실패하는지까지 확인.
- `OBJECTIVE.md` 갱신: 기준1 채점 정정 · 기준3 장애물 이동 · 기준4 참고종수 276→344 · 기준2는 오늘 자동 몫이 아님을 명시.
- 사람 손 넷에 `[사람 아침 5분 ①~④]` 번호를 붙여 한 자리에서 끝나게 묶었다.

## 사람이 할 일 (아침 5분, 순서대로)
1. **T9** D405 앞 20~40cm에 **4점 마커판**을 세운다 → `target_check.py`가 4/4면 성공. (이것 하나가 기준2 전체를 푼다)
2. **T23** 같은 자리에 **무대 조명**. (컬러 평균이 최대노출에도 34.8이라 마커판만 놓으면 또 0/4)
3. **T10** 손목축 → D405 렌즈 중심 **캘리퍼스 3방향**.
4. **T12** **줄자**로 `mount.x` + 가능하면 `mount.y`·`mount.yaw_deg`.

## 다음 한 수
builder **T33**(경로 대체) · tester **T36**(반복오차) · auditor **T37**(기준3 채점). 셋 다 지금 바로 돈다.

## 막힌 것
기준2 전체가 사람 손 넷에 걸려 있고 자동으로 우회할 길이 없다(T20 미결로 확인). **남은 14일의 자동 몫은 기준1·3·4**로 보고 간다. 기준3이 T36에서 15mm 초과가 나오면 그때는 목표의 기준3을 **증거를 근거로** 고쳐야 한다 — 그 판단은 다음 플래너 몫으로 남긴다.
