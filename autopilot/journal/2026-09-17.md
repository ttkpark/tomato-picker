# 자동운전 일지 — 2026-09-17

## 21:51 · planner · 사이클 1 · 5분 · $3.29

## 채점 (졸업 기준 5개)
1. **링크 실측 일치 — 거의.** z0/d0/l1/l2/l3는 08-31 실측이고 `ros_selfcheck`가 강제한다. 안 된 것: `so101_geometry.yaml`의 `mount.x: 60.0`("아직 안 쟀다") → T12
2. **잔차 ≤15mm — 못 믿는다.** 젯슨 `~/arm_eye.json`에 13.934mm가 있으나 5번 재수집에 한 번도 재현 안 된 09-04 일회성 값. 이후 run은 17.8·20.2mm → T8·T9·T10
3. **5/5 도달 — 모름.** `/arm/move_to_point` 시험 기록이 저장소에 전혀 없다. 도구부터 없다 → T7
4. **175종 통과 — 됐다.** 오늘 PC 실측 45/60/70/33 = 208/208, RC=0
5. **태그 — 아직.**

## 한 일 / 확인한 사실
- 젯슨은 **192.168.0.19**(.8은 옛 IP). click-server·depth-cam·astra-cam active, tomato-voice activating. 장치 상태는 안 건드렸다(읽기만).
- ⚠ `.venv`에 `pyyaml`이 없어 `ros_selfcheck`가 `ModuleNotFoundError`로 죽고 있었다. 파이프 뒤 `$?`가 0이라 통과로 오독될 뻔했다. 설치해 해결 → T11
- **T4는 이미 돼 있었다** — `ros_selfcheck.py:303/:312`. `l2`를 1mm 틀자 `FAIL`, RC=1 확인 후 되돌림.
- **인수인계-2026-09-04 §8-1이 "새로 짜야 한다"고 한 `--fix-t`는 이미 있다**(`handeye_resolve.py:334`, 커밋 05f6639, 09-05). 문서가 낡았다.
- **새 단서**: `handeye_from_axes.py`를 젯슨 `~/joint_axes.json`(09-01)에 돌리니 `t=(-98.2, 33.5, 57.8) · |t|=119mm · 거리잔차 7.8mm · 회전 0.68°`. 이행계획이 적은 실물 배치(뒤 7~8cm·위 7~8cm)의 크기 ≈113mm와 맞는다 → **09-04 내내 쓴 `--expect-t-mm 60 100` 게이트가 옳은 해를 버렸을 수 있다**(짐작, 판정=T8). 단 이 데이터는 브래킷 재접착 **전**이다 → T10
- 작업판: T1·T2·T4 닫음 / T5 p1→p2 / T8~T12 만듦. todo=9 done=3 blocked=0. 커밋 `b5a8867` 푸시됨.

## 사람이 할 일 (아침 5분)
- **T9** 손목(wrist_roll)축 중심 → D405 렌즈 중심을 **캘리퍼스로 3방향**(찌르는 방향/왼쪽/위, mm, 축-축 벡터 관례). 이 셋이 기준2·3을 동시에 푼다 — 코드는 이미 준비돼 있다.
- **T12** 차체 중심 → 팔 마운트 평면 앞뒤 거리(`mount.x`). 기준1의 마지막 구멍.

## 다음 한 수
빌더는 **T8**(사람 없이 지금 가능 — 젯슨 `~/handeye_samples.json`을 받아 게이트 3종으로 재풀이, 119mm 해가 버려졌는지 판정). 테스터는 **T10**(현재 하드웨어로 `joint_axis_check` 재수집) 또는 **T7**(5회 시험 도구, `--dry-run`).

## 막힌 것
없음(blocked=0). 다만 기준2·3의 최종 확정은 T9의 사람 실측 없이는 짐작으로만 남는다.

## 22:08 · builder(opus) · 사이클 2 · 7분 · $4.28

## 한 일 (작업 ID와 제목)
- **T3 완료** — 손-눈 잔차 15mm를 코드가 강제한다. `handeye_resolve.py`에 순수 판정 `gate(rms, max_rms)` 추가(합격선은 `handeye.GOOD_RMS_MM` 한 벌, `--max-rms`로 조절, 경계 포함). 불합격이면 `main()`이 **RC=1**, `--save`도 차단(`--save-anyway`로만 강행). `ros_selfcheck`에 `[손-눈 합격선]` 검사군 8종 추가. 커밋 `a5a018f`.
- **T8 완료** — 게이트 판정을 `docs/인수인계-2026-09-04.md` **§20**에 표로 남김(§10은 09-05가 이미 씀). 커밋 `547fa1d`.
- **T13 만듦**(tester p1): T9 실측값은 새 표본에 넣어야 한다.

## 확인한 사실 (명령 + 출력 숫자)
- `python ros2/tools/ros_selfcheck.py` → **✅ 78개**, RC=0 (70→78. 플래너 메모는 71을 예상했으나 경계값만 재면 배선이 끊겼을 때 못 잡아 8종으로 늘렸다). 4종 합계 45+60+78+33=**216**, 전부 RC=0.
- `handeye_resolve.py <젯슨표본>` → `❌ 잔차 20.24mm > 합격선 15.0mm`, **RC=1**, `저장 안 함:`(파일 안 생김). `--max-rms 25` → RC=0.
- **T8 판정 = 아니오.** 09-04 22:54 표본(11개) 후보 넷의 |t| = 435 / 349 / 449 / 350mm — 전부 범위 밖이라 게이트 없음 / `60 100` / `90 140` **셋 다 같은 해**(roll+1·무게중심, 20.24mm, 435mm)로 끝난다. 게이트가 옳은 해를 버린 게 아니다.
- 곁가지: `--fix-t (-98.2,33.5,57.8)`=76.68mm · `(-80,0,80)`=69.82mm · `(0,0,0)`=**80.85mm** → 113~119mm 부근이 통째로 나쁘다(데이터가 그 자리를 지지하지 않는다). `--fit-geom`은 l1+20/l2−20 한계까지 달아나며 20.2→17.3mm뿐. 잔차와 |r|>0.6인 관절 **없음**(최대 lift +0.53).
- roll 부호는 이 표본에서 뚜렷(어긋남 +1=2.61° vs −1=32.10°) — `handeye_collect.py:131` 주석의 반대 결론은 **다른(저녁) 표본** 것이다.
- 젯슨은 `scp`/`ssh ls`만 했다. 서비스·장치 상태 안 건드림.

## 다음 한 수 (다음 사이클이 칠 첫 명령)
- 빌더: `python autopilot/board.py next --role builder --claim` → **T11**(의존성 못 박기, `pyyaml`·`numpy`).
- 테스터: **T9**(joint_axes 재수집) 또는 **T7**(`--dry-run` 5회 시험 도구). **T13은 T9/T10 뒤에만 의미가 있다.**
- 사람 아침 5분: **T10**(캘리퍼스 3방향) · **T12**(`mount.x`) — 여전히 유일한 열쇠.

## 막힌 것
없음. 다만 §20이 새로 못 박은 사실 하나: **낡은 `~/handeye_samples.json`에 실측값을 넣어 보는 길은 닫혔다.** T9 뒤엔 반드시 새 채집(T13)을 거쳐야 기준2가 판정된다.

## 22:18 · tester(sonnet) · 사이클 3 · 8분 · $2.86

## 자체검증  45/45 · 60/60 · 78/78 · 33/33 (216/216, RC=0 전부)

## 한 일 / 확인한 사실
- T9(joint_axis_check.py 5축 전부) 시도 → **표본 0개, 전부 실패**. `target_check.find_dots()`가 찾는 흰 종이 4점 마커판이 지금 D405 시야에 없고(면봉형 막대만 보임), 이 검사는 사람이 마커판을 다시 놔야 돈다. tomato-voice는 원상복구(active)해 두고 todo로 되돌림.
- T10은 애초에 캘리퍼스 실측이 필요한 `[사람]` 작업이라 착수 없이 todo로 되돌림.
- T7 완료: `ros2/tools/move5_check.py` 신설. `--dry-run`은 ROS 없이 `kinematics.inverse/forward`로 도구 자체(기록형식·성공판정·RC)를 검증(5/5 성공 1회, reachable+unreachable 혼합 1/2 성공 1회 모두 정확). 실기 경로(rclpy 연결)는 코드는 있으나 젯슨에서 ROS2 스택을 못 띄워봐서 미검증.

## 넘긴 작업(ID)
T7 done(tester). T9 todo로 복귀 + 막힌 이유 기록. 커밋 `21325b3` 푸시됨.

## 막힌 것
T9는 **사람이 4점 마커판을 D405 시야에 재배치**해야 다음 사이클이 이어받을 수 있다(T10·T12와 함께 사람 대기 3건).

## 23:40 · builder(opus) · 사이클 4 · 7분 · $5.03

Done. T11 closed, all green.

## 한 일 (작업 ID와 제목)
- **T11 완료** — PC 자체검증이 빈 환경에서도 "무엇이 없는지 말하고" 죽게 만들었다.
  - `tools/selfcheck_deps.py` 신설(`require`/`missing`/`PIP_NAME`). 없으면 **`pip install …` 한 줄 + 종료코드 2**. **2 = 환경이 없다 / 1 = 검사 실패**로 갈랐다 — 09-17에 이 둘이 섞여 "통과"로 오독될 뻔했다.
  - 4종 전부 배선: `handeye_check`→`require("numpy")`, `eye_check`→`require("numpy","cv2")`, `ros_selfcheck`→`require("numpy","yaml")`, `arm_cartesian_check`는 **서드파티가 하나도 없다**(주석으로 못 박고 검사가 지킨다).
  - `requirements.txt`에 **PyYAML>=6.0** 추가 + "자체검증만 돌리려면 이 셋" 머리말.
  - `ros_selfcheck`에 `[의존성]` 검사군 신설(78→**95종**): requirements 명시 여부 · 종료코드 2 · pip 이름 환산(`yaml`≠`pip install yaml`) · **스크립트가 묻는 목록 = 실제 필요한 것** · require가 import보다 앞인가.
- **T14 만듦**(planner p3): OBJECTIVE 기준4의 "175종"이 낡았다(실제 233).

## 확인한 사실 (명령 + 출력 숫자)
- **빈 venv**(3.13.7, pip만): `handeye_check` RC=**2** `pip install numpy` / `eye_check` RC=**2** `pip install numpy opencv-python` / `ros_selfcheck` RC=**2** `pip install numpy PyYAML` / `arm_cartesian_check` RC=**0** 33/33(서드파티 없음 실증).
- **`pip install numpy PyYAML opencv-python`만 깐 뒤**: **45 / 60 / 95 / 33 = 233종, 전부 RC=0.** 프로젝트 `.venv`에서도 같은 숫자.
- **eye_check는 cv2도 필요하다** — `Eye.fruits()`가 `..vision.color_detect`를 늦게 끌어오고(`eye.py:496`) 그것이 cv2를 쓴다. 파일 머리만 훑은 내 첫 표는 틀렸고, **빈 venv에서 60종 중 뒤쪽 `test_two_cameras`에서 RC=1로 죽는 것**을 보고 고쳤다(이게 딱 "검사 실패"로 오독되는 자리다).
- 음성 검사: `requirements.txt`에서 PyYAML 줄을 지우자 `FAIL requirements.txt가 PyYAML를 적어 뒀다`, RC=1 → 되돌림 확인.
- 젯슨은 이번 사이클에 **안 건드렸다**(장치·서비스 상태 변경 없음).
- **INBOX 확인**: 2026-09-17 22:15 사람 줄을 읽었다 — 체크하고 `→ 처리:` 붙였다.

## 다음 한 수 (다음 사이클이 칠 첫 명령)
- 빌더: `python autopilot/board.py next --role builder --claim` → 남은 빌더 일은 **T5**(평행이동 오차 문서화, T9/T10/T13 뒤)뿐이라 "(없음)"이 나오면 감사자 지적에서 하나 만들어라.
- 테스터: **T9**는 여전히 사람이 4점 마커판을 D405 시야에 놓아야 돈다 → 막혀 있으면 **T6**(ros.1 표 감사)로.
- 사람 아침 5분: **T10**(캘리퍼스 3방향) · **T12**(`mount.x`). 이 셋이 기준1·2·3의 유일한 열쇠다.

## 막힌 것
없음. 다만 졸업기준4의 "175종"이라는 표현이 검사를 늘릴 때마다 낡는다 — T14로 넘겼다(플래너만 OBJECTIVE를 고친다).
