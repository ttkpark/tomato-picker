# ROS 2 계통 이행 계획 — 단계 · 브랜치 · 태그

> 새 계통이 **왜** 필요한지는 [`ros2/README.md`](../ros2/README.md)에 있다.
> 이 문서는 **언제 무엇을 하고, 어디에 이름을 붙이는가**를 정한다.
>
> 시작: 2026-08-28 · 브랜치 `feat/ros2-arm-estimation`

---

## 1. 두 계통은 나란히 산다

```
master ──● v1.0.0 ─────────────────────────────▶   기존 스택 (데모가 도는 쪽)
          \
           ●── v2.0.0-ros.1 ── ros.2 ── ros.3 ──▶   ROS 계통 (만드는 쪽)
```

**기존 스택을 지우지 않는다.** 이유는 감상이 아니라 일정이다 — 부스 데모는 지금
도는 것으로 하고, 새 계통은 옆에서 자란다. 둘이 같은 하드웨어를 쓰므로 동시에
켤 수는 없지만(포트는 한 프로세스만), `systemctl stop` 한 줄로 오간다.

새 계통이 기존 스택의 **모든 기능을 대체하면** 그때 `v2.0.0`을 달고 master를
옮긴다. 그 전까지 master는 언제나 "지금 데모에서 도는 것"이다.

## 2. 태그 규칙 — 계통이 태그 문자열에서 드러나야 한다

SemVer 하나로 통일하되, 새 계통은 **프리릴리스 라벨**로 갈라 둔다.

| 계통 | 형식 | 예 | 다는 곳 |
|---|---|---|---|
| 기존 스택 | `v1.MINOR.PATCH` | `v1.0.0`, `v1.1.0` | `master` |
| ROS 계통 | `v2.0.0-ros.N` | `v2.0.0-ros.1` | `feat/ros2-*` |
| 대체 완료 | `v2.0.0` | | master (그때) |

**왜 접두사(`ros/v0.1`)가 아니라 프리릴리스인가** — 순서가 자동으로 맞기 때문이다.
SemVer 우선순위에서 `1.0.0 < 2.0.0-ros.1 < 2.0.0`이므로, 도구가 정렬하든 사람이
읽든 "새 계통은 v2를 향해 가는 중"이 한눈에 보인다. 접두사로 나누면 두 계열의
선후관계를 사람이 외워야 한다.

```bash
git tag -l 'v1.*'            # 기존 계통만
git tag -l 'v2.0.0-ros.*'    # ROS 계통만
git describe --tags --match 'v2.0.0-ros.*'    # 지금 어느 이정표 뒤에 있나
tools/tag.sh v2.0.0-ros.2 "손-눈 보정 실측 통과"
```

**규칙 셋**

1. **계통을 섞어 달지 않는다.** ROS 브랜치에 `v1.x`를 달지 않고, master에
   `-ros.N`을 달지 않는다. 태그는 그 커밋이 **어느 계통의 이정표인지**를 말한다.
2. **이정표에만 단다.** 커밋마다 달지 않는다. 아래 §3의 표가 곧 태그 목록이다.
3. **되돌릴 수 없다고 생각하고 단다.** 태그를 밀어 놓고 옮기면(force) 남의 클론이
   조용히 다른 것을 가리킨다. 잘못 달았으면 지우지 말고 다음 번호를 단다.

## 3. 단계 — 각 단계는 **혼자서도 쓸모가 있다**

[`docs/보드-계약.md`](보드-계약.md) §13과 같은 원칙이다. 한 단계가 끝나면 그
상태로 멈춰도 손해가 아니어야 한다.

| 태그 | 무엇이 되는가 | 어떻게 확인하는가 |
|---|---|---|
| `v2.0.0-ros.1` | **골격.** URDF/TF, 팔·주행 브리지, D405 검출, 손눈보정, 자체검증 | `ros2/tools/ros_selfcheck.py` 전부 통과 · rviz에 팔이 실제 자세로 뜬다 |
| `v2.0.0-ros.2` | **실측.** 링크 길이·마운트를 자로 재고, 손눈보정 잔차 ≤15mm, 좌표로 보내면 집게가 거기로 간다 | 임의의 자리에 둔 열매를 `/arm/move_to_point`로 5회 중 5회 스탠드오프까지 도달 |
| `v2.0.0-ros.3` | **2단계 — 시각 서보잉.** 마지막 몇 mm를 눈으로 닫는다 | 링크 길이를 일부러 5% 틀리게 해도 집힌다 |
| `v2.0.0-ros.4` | **3단계 — MoveIt.** 충돌 회피·궤적 | 줄기 사이로 들어가 집는다 |
| `v2.0.0` | 기존 스택 대체 | 음성·대시보드·라인주행이 ROS 위에서 돈다 |

⚠ **기준3의 '도달'은 여러 걸음이어도 된다 — 쪼개는 일은 `cartesian.travel_to()`가
한다**(2026-09-18에 정함). `/arm/move_to_point` 한 번에 목표까지 가지만, 팔은
한 걸음 80mm(`ARM_CART_MAX_STEP_MM`, 좌표 오타 방어) 이하로만 움직이고 **걸음마다
바닥·몸통·사거리·관절한계 검사를 다시 받는다.** 상한을 키워서 푸는 것은 금지다.
왜·어떻게 = [`docs/arm-cartesian.md`](arm-cartesian.md) §4.

### 기준1 채점표(2026-09-20, 감사 사이클 438 종합 갱신)

기준1 = "링크 길이·마운트를 자로 잰 실측값으로 URDF와 `kinematics.py`가
일치한다." 2026-09-18 초안 이후 T32(마운트 강제), T42(줄끝 강제), 사이클 435(계측 경계 확립),
사이클 438(URDF xacro 기본인자 25종 및 limits_deg 6관절 유효성 강제)을 거치며
`so101_geometry.yaml`의 모든 항목에 강제 검사가 완비되었다.

| 키 | 강제하는 검사 | 실측 여부(근거) |
|---|---|---|
| `arm.z0/d0/l1/l2/l3` | ✅ `ros_selfcheck.test_geometry_matches`(yaml↔`ArmGeometry` 기본값 1e-9 일치) + `test_urdf_matches_kinematics`(so101_arm.xacro FK 7자세 대조) + `tomato_robot.urdf.xacro` 기본인자 5종 대조 | 실측. yaml 주석 "2026-08-31 실측"(`so101_geometry.yaml:16`) |
| `arm.limits_deg`(6관절) | ✅ `ros_selfcheck.test_geometry_matches`가 6개 전 관절(`kin.JOINTS + ('gripper', )`)의 정의 및 `min < max` 유효 범위를 코드로 강제 + `tomato_robot.urdf.xacro`의 12개 인자(`*_min_deg`, `*_max_deg`) 기본값과 1e-6 완전 일치 강제 | 설계상 분리. yaml 자신이 "⚠ 추정치다"라고 명시(`:24`) — 실제 안전 한계는 대시보드 캘리브레이션(`arm_cartesian.json`)이 별도 소유하며, URDF 모델의 물리적 유효성 및 launch/xacro 기본값 일치는 코드로 완전 보장됨 |
| `mount.x` | ✅ `ros_selfcheck.test_mount_compare`(TF↔yaml 1.0mm 허용오차 대조 19종) + `test_geometry_matches`(예상 물리범위 40~80mm 검사 + "아직 안 쟀다(T12 대기)" 주석 강제) + `tomato_robot.urdf.xacro` 기본인자 대조 | 미실측(T12 대기). **`ros.2`에서는 IK 사슬(`tool→arm_base`)에 들어오지 않으므로 `ros.3` 선행 요건으로 격하되어 `ros.2` 기준1 합격을 가로막지 않음.** |
| `mount.y` | ✅ `mount_compare.py` 대조 + `test_geometry_matches`(예상 물리범위 -10~+10mm 검사 + "좌우 대칭 가정치" 주석 강제) + `tomato_robot.urdf.xacro` 기본인자 대조 | 좌우 대칭 가정치(0.0mm). 미실측, T12 실측 시 검증. |
| `mount.z` | ✅ `mount_compare.py` 대조(1.0mm 허용) + `test_geometry_matches`(예상 물리범위 70~85mm 및 mm 단위 검사 + "2026-08-31 실측" 주석 강제) + `tomato_robot.urdf.xacro` 기본인자 대조 | 실측. 2026-08-31 실측 76.5mm(지면 → 마운트 평면). |
| `mount.yaw_deg` | ✅ `mount_compare.py` 대조(0.5° 허용) + `test_geometry_matches`(예상 물리범위 -5~+5° 검사 + "브래킷 정면 정렬 가정치" 주석 강제) + `tomato_robot.urdf.xacro` 기본인자 대조 | 브래킷 정면 정렬 가정치(0.0°). 미실측, T12 실측 시 검증. |
| `base.length/width/height/wheel_radius` | ✅ `test_geometry_matches`(차체 치수 4종에 "시각화용 가정치" 및 T12 환산식 $mount.x = L/2 - d_{\text{front}}$ 사용 주의 주석 명시 강제) + `tomato_robot.urdf.xacro` 기본인자 4종 대조 | 시각화용 가정치. 주행 계산이나 팔 기구학에 미사용. T12 실측 시 실제 차체 길이 $L$ 병행 실측 필요. |

**결론**: 링크 길이 5종은 **실측 + 3중 강제**(ArmGeometry, xacro FK 7자세, URDF 기본인자)로 합격. 마운트 4종은 **TF↔yaml 강제 및 물리 경계 검증이 완비**되었으며, 미실측 항목(`mount.x`)은 `ros.3` 선행으로 분리되어 `ros.2` 기준1 요건을 충족함.

#### T32 이후 — 마운트에 **실제로 실패하는** 강제가 생겼다 (2026-09-18, builder)

위 표의 `mount.*` 네 줄이 "강제 없음/약함"이었던 것은 `tf_check.py`의 [마운트]
절이 `any(abs(v) > 1e-6)`, 즉 **0이 아니기만 하면** 통과시켰기 때문이다. 이제
`base_link→arm_base` TF의 **평행이동 3성분과 요**를 `so101_geometry.yaml`과
직접 비교한다(허용 1.0mm / 0.5도). 요는 쿼터니언에서 뽑고, roll·pitch가
0인지도 같이 본다 — xacro는 `rpy="0 0 yaw"`로 박혀 있어서 기울었다면 다른
경로가 끼어든 것이다.

- 계산은 `ros2/tools/mount_compare.py`(rclpy 없음)로 떼어 놨다. `tf_check.py`는
  rclpy를 물어 PC에서 못 도는데, **검사가 실패할 수 있는지는 PC에서 확인해야**
  하기 때문이다(기준4 §T39의 문제의식). `ros_selfcheck.py` [마운트] 19종이
  1.5mm·0.6도·키 누락·2도 기울기를 넣어 **실패하는 것까지** 확인한다.
- 읽는 yaml은 **런치가 읽은 설치본**(`ament_index`의 share)이다. 소스만 고치고
  빌드를 안 한 상태를 통과시키지 않기 위해서다. 젯슨은 symlink-install이라
  설치본이 소스를 가리킨다(실측: `/ws/install/.../so101_geometry.yaml ->
  /ws/src/.../so101_geometry.yaml`).

**실제 실행 기록 (젯슨 192.168.0.19, 도커 `tomato-ros:jazzy`,
`sudo HOME=/home/server docker compose run --rm -T ros bash /ws/tools/bringup_check.sh`)**

```
[마운트] base_link → arm_base 가 so101_geometry.yaml과 같은가
  (읽은 yaml: /ws/install/tomato_description/share/tomato_description/config/so101_geometry.yaml)
  ok   mount.x        (TF 60.00 vs yaml 60.00 mm · 차이 0.00)
  ok   mount.y        (TF  0.00 vs yaml  0.00 mm · 차이 0.00)
  ok   mount.z        (TF 76.50 vs yaml 76.50 mm · 차이 0.00)
  ok   mount.yaw_deg  (TF 0.000 vs yaml 0.000 도 · 차이 0.000)
  ok   마운트가 기울지 않았다 (roll=pitch=0)
✅ 전부 통과 (15개) → ✅ 0~2단 전부 통과
```

**2026-09-18 (builder, T42) — 같은 명령이 이제 손질 없이 돈다.** 위 기록은 젯슨에서
`sed -i 's/\r$//' ros2/tools/*.sh`를 먼저 걸어야 나왔다(그 파일들이 CRLF라 컨테이너
bash가 통째로 못 읽었다). 입구를 막았다 — 저장소 루트 `.gitattributes`가 배포되는
종류를 `text eol=lf`로 못 박고, `ros_selfcheck` **[줄끝]** 13종이 강제한다. 재확인:

```
$ sudo HOME=/home/server docker compose run --rm -T ros bash /ws/tools/bringup_check.sh
✅ 전부 통과 (15개) → ✅ 0~2단 전부 통과   ·   RC=0   (손질 없음, 03:1x)
```

**그 검사가 진짜인지도 실기에서 확인했다**(돌연변이 시험). xacro의 마운트 z를
`${($(arg mount_z_mm)+5.0)*mm}`로 5mm 틀어 URDF만 yaml과 어긋나게 만들고 같은
명령을 돌렸더니 —

```
  FAIL mount.z  TF 81.50 vs yaml 76.50 mm · 차이 5.00 (허용 1.0mm)
❌ 1개 실패 / 15개 중   (bringup_check 종료코드 1)
```

xacro는 즉시 되돌렸다(`diff` 동일 확인). 이로써 위 표의 `mount.x/y/z/yaw_deg`
줄의 "강제하는 검사"는 **✅ `tf_check.py` [마운트] + `ros_selfcheck` [마운트]**로
바뀐다. **실측 여부는 그대로 ❌다** — 강제가 생겼다는 것은 "yaml에 적힌 숫자와
TF가 같다"는 뜻이지 그 숫자가 자로 잰 값이라는 뜻이 아니다(T12가 그 일).

#### 2026-09-18 (사람 판단) — **마운트 넷은 `ros.2` 합격 요건이 아니다**(`ros.3` 선행으로 내린다)

기준1을 "링크 길이 + 마운트"로 채점해 온 것이 `mount.x` 하나 때문에 기준1을
계속 "절반"으로 묶어 두고 사람의 아침 5분을 잡아먹고 있었다. 사람이 07:17에
근거와 함께 격하를 지시했고, 코드로 확인했다:

- `ros2/src/tomato_bridge/tomato_bridge/arm_node.py:68` — `base_frame` 기본값이
  **`arm_base`**다.
- 같은 파일 `:155` — 목표점을 `_to_arm_base_mm()`으로 **`arm_base`에 옮겨** IK에 넣는다.

즉 지금 도는 사슬은 `카메라 → tool → arm_base`이고 **`base_link`를 지나지 않는다.**
`mount.x/y/yaw_deg`는 IK 결과에 **한 mm도 안 들어온다.** 이 넷이 실제로 걸리는 곳은
둘뿐이다: (가) rviz에서 팔이 차체 위 엉뚱한 자리에 그려지는 것(보기의 문제)
(나) **주행 뒤 좌표를 다시 쓰는 `ros.3` 이후**(그때는 `base_link`가 사슬에 들어온다).

**그래서 기준1은 `ros.2`에서 링크 길이(`z0·d0·l1·l2·l3`)로만 채점한다** — 그 다섯은
실측이고 `test_geometry_matches` + `test_urdf_matches_kinematics` + `bringup_check`
15/15가 강제한다(사이클49 재확인). 위 표의 `mount.*` 네 줄은 **"`ros.2` 합격 요건
아님 — `ros.3` 선행"**으로 읽어라. 강제(TF↔yaml 일치)는 그대로 살아 있다: 지금
적힌 숫자가 바뀌면 검사가 실패한다. 바뀌는 것은 "그 숫자가 실측이어야 하는 시점"뿐이다.
작업판 T12는 p2 → **p4**로 내리고 blocked를 유지하며, **사람 아침 목록에서 뺐다.**

##### `ros.3`에서 먼저 결정할 것 — `base_link` 원점이 어디인가

지금 `so101_geometry.yaml`은 `base_link` 원점을 **"차체 중심(몸통 상자의 중심)"**
으로 적어 두었다. 그런데 주행이 실제로 도는 축은 **네 바퀴 접지점 사각형의 중심**
이다. 둘이 다르면 "제자리 회전" 뒤 표적 좌표가 TF의 예측과 어긋난다 — CLAUDE.md의
"회전 1도마다 진행축을 17px 밀어낸다"(회전 중심이 카메라에서 벗어나 있다)와 **같은
뿌리**이고, `docs/보드-계약.md:429`도 `wmax`의 기준점을 회전 중심으로 정의하라고
적어 두었다.

**결정 필요(사람이 정한다)**: `base_link` 원점 = 몸통 중심인가, 바퀴 사각형 중심인가.
**바퀴 중심이 유력하다** — 재기도 바퀴 축 기준이 쉽고(줄자를 바퀴 축에 댄다) 주행
물리와도 맞는다. 지금 고치지 않는 이유는 `ros.2`에서 이 값이 IK에 안 들어오기
때문이다(위 문단). `ros.3`에 들어가기 전에 정하고, 정한 뒤에 T12를 되살려라 —
**무엇을 재야 하는지가 이 결정에 달려 있다.**

**같이 드러난 것**: 젯슨의 `ros2/tools/*.sh`가 전부 **CRLF**라 컨테이너 bash가
`set: pipefail: invalid option name` / `$'\r': command not found`로 죽는다.
이 저장소는 Windows에서 작업해 git이 체크아웃할 때 CRLF로 바꾸는데
(`git ls-files --eol` = `i/lf w/crlf`), 배포가 그 작업트리를 그대로 scp한다.
이번엔 젯슨에서 `sed -i 's/\r$//'`로 풀고 돌렸다 → 항구적 해결은 T42.

### 기준2 채점표(2026-09-20, 감사)

기준2 = "손-눈 보정 잔차 ≤15mm (`handeye_resolve.py`가 찍는 residual)."
기준1·3·4 채점표에 이어, 기준2가 (a) 코드로 어떻게 강제되는지 (b) 실측과 가정이
어디까지 확인되었는지 (c) 현재 블로커의 실체가 무엇인지를 대조해 채점했다.

| 항목 | 판정 | 근거 |
|---|---|---|
| ① 15mm 문턱 강제 | ✅ 완벽 | `handeye_resolve.py:gate()`가 `GOOD_RMS_MM=15.0mm`로 판정. 15.00mm 이하는 통과, 15.01mm 이상은 거절 문구 반환(`gate()` 순수함수 분리). 종료코드 `return 0 if ok else 1`로 실패 시 비영 반환 강제(`ros2/tools/handeye_resolve.py:617`). `ros_selfcheck.py` [손-눈 합격선] 8종이 14.99/15.00/15.01mm 경계 및 비영 종료코드 반환을 코드로 강제 검사함. |
| ② 불합격 해의 저장 방어 | ✅ 완벽 | 잔차가 15mm 초과 시 `--save` 파일 저장을 차단(`handeye_resolve.py:586-589`), 강제 저장 플래그(`--save-anyway`) 없이는 `~/arm_eye.json`을 오염시키지 못함. `ros_selfcheck.py`가 이 가드를 코드로 검증. |
| ③ 도구 좌표계 원점 규약 | ✅ 일치 | `handeye_resolve.tool_frame()`과 `handeye_collect.tool_frame_from_joints()` 모두 `kin.forward()`(TCP = 집게가 무는 지점)를 원점으로 사용(`ros_selfcheck` 2종 강제). 작업판 T10의 옛 제목("손목축 중심")은 착오였음이 규명되었고, 원점이 TCP임이 코드로 못 박힘. |
| ④ 평행이동 벡터(t_tool_cam) 실측 | ✅ 실측완료 | 사람이 캘리퍼스로 TCP → D405 컬러 광학 중심 3성분을 실측함(2026-09-18, `docs/인수인계-2026-09-04.md` §28): APPROACH=−52.0mm, LATERAL=0.0mm, UP=+59.0mm (`|t|=78.7mm`). |
| ⑤ `--fix-t` 풀이 알고리즘 | ✅ 검증완료 | `handeye_resolve.py:solve_fixed_t()`가 입력된 t_x를 정확히 고정하고 회전 R_x 3자유도만 훑음. 합성 데이터 정답 t_x 입력 시 rms < 0.5mm 수렴, 20mm 오프셋 입력 시 rms 증가함을 `ros_selfcheck.py:test_handeye_identifiability` [손-눈 식별성]에 추가해 검증 완료(2026-09-20 감사). |
| ⑥ 현 하드웨어 데이터 통과 여부 | ❌ 미달 (0/1) | 구 09-04 표본(`~/handeye_samples.json`)은 어느 벡터를 넣어도 잔차 70~81mm(실측치 입력 시 72.64mm)로 표본 자체의 심각한 왜곡 확인(브래킷 재접착·재교시 이전 데이터). **신규 표본(T13) 채집 전까지 기준2는 미통과 상태.** |

#### 확인된 사실 및 작업판 상태 정정
1. **T10 실측은 이미 끝났다**: 작업판에 T10이 `[사람]`으로 열려 있어 이후 사이클들(테스터/빌더)이 "사람 물리 실측 대기 중"으로 오인하고 사이클을 공전시켰다. 사람은 2026-09-18에 이미 실측값(−52, 0, +59 mm)을 §28에 남겼다.
2. **임계경로의 실체는 T9/T65 → T13이다**: 사람의 물리 실측이 아니라, D405 시야 내 마커판 각도 정렬(스침각 71.9° → 법선 30° 이내로 축소) 후 젯슨에서 `handeye_collect.py`로 신규 표본을 채집하는 것이 유일한 임계경로다. 새 표본 채집 후 `handeye_resolve.py --fix-t -52 0 59`로 풀었을 때 비로소 잔차 ≤15mm 졸업 판정이 가능해진다.

### 기준3 채점표(2026-09-20, 감사 사이클 428 종합 갱신)

기준3 = "임의의 자리에 둔 표적을 `/arm/move_to_point`로 5회 중 5회 스탠드오프까지 도달 (오차 ≤ 15mm)."
2026-09-18 T37 채점 이후 T41(하중 경계), T45(처짐 되먹임), T49/T56(prep escape.walk),
T58(기록 격리/회수), T59(prep 실패 시 0/5 방지), T64(관절값 로깅), T71/T75/T76(arm_load_limits 졸업 무효화) 등
대대적인 코드 보완이 완료되었다. 사이클 428 감사를 통해 (a) 15mm 도달 문턱 및 5/5 강제,
(b) 거짓 졸업 차단, (c) 물리 및 경로 보장 장치, (d) 실기 통과 현황을 전수 대조했다.

| 항목 | 판정 | 근거 |
|---|---|---|
| ① 15.0mm 도달 오차 문턱 강제 | ✅ 완벽 | `move5_check.py:SUCCESS_ERR_MAX_MM=15.0` 상수를 정의하고, `run_real`(`:732`) 및 `run_dry`(`:455`) 모두 `err <= SUCCESS_ERR_MAX_MM`으로 통과 여부를 판정. `ros_selfcheck.py` [표적] 3종이 상수 정의, 부등식 판정, 전회차 성공 반환을 코드로 강제 검증. |
| ② 5/5 전회차 성공 및 RC 강제 | ✅ 완벽 | `move5_check.py:858`이 `return 0 if ok == len(points) else 1`로 5회 중 단 1회라도 실패 시(4/5 등) 비영(RC=1) 종료코드를 반환하도록 못 박음. |
| ③ 거짓 졸업 방어 (Graduation Block Guard) | ✅ 완벽 | `move5_check.py:is_graduation_blocked()`가 `load_limits`가 `None`, `"none"`, 또는 `"config.*"`일 때 `graduation_blocked=True`를 반환. 코드 기본값으로 5/5를 얻더라도 `arm_load_limits.json 없음 — 이 판은 기준3 졸업 인정 불가` 경고를 출력하고 기록 줄마다 `graduation_blocked=true`를 영구 각인(`ros_selfcheck.py` [표적] 5종 강제). |
| ④ 시작 자세 가드 및 prep 탈출 | ✅ 완벽 | A자세(`signed_radius < 90mm`)에서 출발 시 `run_prep()`이 `hardware/escape.py:walk()`를 통해 1걸음 ≤11°로 매번 실측을 되읽으며 뻗음(`signed_r ≥ 90mm`). prep 실패 시 5회를 강행하지 않고 `rc=-2`로 조기 종료하여 거짓 0/5 기록 오염을 방지(`ros_selfcheck` [뻗기] 10종 강제). |
| ⑤ 직교 경로 분할 및 관절 공간 대체 | ✅ 완벽 | `cartesian.travel_to()`가 이동을 80mm 단위(`ARM_CART_MAX_STEP_MM`)로 쪼개며, 중간점에서 관절 한계(elbow_flex 음수 등)에 걸리면 자동으로 `joint_travel()`(스텝 ≤12°, EE ≤80mm)로 우회(`arm_cartesian_check.py` [직선이 막히면 다른 길로] 21종 검증). |
| ⑥ 마지막 걸음 처짐 되먹임 (Settle) | ✅ 완벽 | `cartesian.settle()`이 이동 완료 후 2초 안정화 뒤 각도 오차를 측정하고, 오차 > 0.5° 시 최대 3회 초과 지령을 인가하여 중력 처짐을 보정(`arm_cartesian_check.py` [되먹임] 20종 검증: 처짐 오차 18.2mm → 1.7mm 수렴). |
| ⑦ 기하 출처 일원화 | ✅ 완벽 | 표적 생성(`sample_points`)과 오차 역산(`run_real`/`run_dry`) 모두 `move5_check._arm_node_geometry()`를 호출하여 `~/arm_cartesian.json` 및 `ArmGeometry` 기본값과 정확히 일치(`ros_selfcheck` 강제). |
| ⑧ 시험기록 격리 및 회수 보증 | ✅ 완벽 | `--dry-run`은 임시 디렉터리(`practice`)에 기록하여 실제 원장(`docs/시험기록/`) 오염을 차단(T43). 젯슨의 volatile 디렉터리 실행 시 `record_pull.py` 명령을 콘솔에 찍어 기록 유실을 방지(T58). |
| ⑨ 현 하드웨어 데이터 통과 여부 | ❌ 미달 (3/5) | T57(seed=0)에서 3/5 최초 완주를 달성했으나, T63(seed=1,2)은 elbow_flex 음수 한계로 0/5에 그침. **현재 젯슨은 QEMU 가상화 상태로 실물 서보가 미연결되어 있어 5/5 실기 측정 및 `~/arm_load_limits.json` 실측 채집 전까지 기준3은 미통과 상태.** |

#### 확인된 사실 및 향후 조치
1. **기준3 소프트웨어 검증 체계는 100% 완성됨**: 표적 선정, prep 탈출, 직교/관절 이중 경로, 처짐 보정, 15.0mm 문턱 판정, 5/5 종료코드, config 태그 무효화까지 모든 소프트웨어 방어벽이 코드로 완비되었고 자체검증 4종 전수 초록으로 뒷받침된다.
2. **남은 유일한 장애물은 실기 하드웨어 연결 및 부하 경계 실측이다**: 물리 서보가 연결되면 (1) A자세 출발 기준 seed별 5회 연속 시험을 수행하고, (2) 이 팔의 실측 한계를 반영한 `~/arm_load_limits.json`을 젯슨에 생성하여 `graduation_blocked=False`인 5/5 성적표를 확정해야 비로소 기준3 졸업이 성립한다.

### T54 실기 5회 (2026-09-18, 사이클 42 tester) — **0/5, 그러나 팔의 점수가 아니다**

젯슨에서 `docker compose run -d ros`로 stage1(팔만, `arm:=true` 기본값)을 띄우고
`python3 /repo/ros2/tools/move5_check.py`를 인자 없이 그대로 돌렸다(REPO 경로
해석 때문에 `/repo` 쪽 파일로 실행, 기록은 `TOMATO_RECORD_DIR`로 host-home 마운트를
거쳐 실제 `docs/시험기록/move-to-point-2026-09-18.jsonl`에 남게 했다 — `/repo`는
읽기전용이라 기본 경로로는 못 쓴다).

**`load_limits`는 이번부터 전 회차 `"config.ARM_LOAD_R_MAX"`**(T41이 실기에서도
동작 확인, "none" 아님). **`prep.extended`는 5회 전부 `false`**다 — T49가 "이번이
첫 실기 확인"이라던 것이 실제로는 **실패했다**:

| 회 | 목표 (x,y,z) | stage | 오차 | 비고 |
|---|---|---|---|---|
| 1 | 265.2,181.9,355.8 | pose | – | prep 실패로 시작 자세 그대로(신호 r=-221.7mm), 가드 즉시 거절 |
| 2 | 315.3,-12.9,400.3 | pose | – | 〃 |
| 3 | 291.3,-116.6,372.4 | pose | – | 〃 |
| 4 | 219.1,-75.4,481.4 | pose | – | 〃 |
| 5 | 96.4,84.7,529.4 | pose | – | 〃 |

### T57 실기 5회 (2026-09-18, 사이클 45 tester) — **3/5, 자동 루프의 첫 완주 판정**

T56(escape.walk 도입) 뒤 같은 명령을 인자 없이 그대로 돌렸다: `docker compose run -d
--name tomato-t57 ros` → 컨테이너 안에서 `ros2 launch tomato_bringup stage1.launch.py
arm:=true` 백그라운드 → `TOMATO_RECORD_DIR=/host-home/tomato-picker/docs/시험기록
python3 /repo/ros2/tools/move5_check.py`(인자 없음, `arm_extend`는 따로 안 돌렸다).

`prep.extended = true`, `signed_r_after = 237.4mm`(가드 90mm를 넘었다 — **T56이
실기에서 처음으로 검증됐다**, 28걸음). `load_limits = config.ARM_LOAD_R_MAX`.

| 회 | 목표 (x,y,z) | stage | 오차(mm) | 판정 |
|---|---|---|---|---|
| 1 | 265.2,181.9,355.8 | – | 5.21 | 성공 |
| 2 | 315.3,-12.9,400.3 | – | 12.06 | 성공 |
| 3 | 291.3,-116.6,372.4 | – | 3.31 | 성공 |
| 4 | 219.1,-75.4,481.4 | joint | 91.16 | 실패(94mm 남기고 더 안 감 — z가 높다) |
| 5 | 96.4,84.7,529.4 | joint | 102.69 | 실패(108mm 남기고 더 안 감 — z가 높다) |

**3/5** — 실패 둘은 z=481/529mm로 사이클36(T40)이 잰 물리적 z 천장(어깨기준
도달 z≈363mm 근방)을 넘는 표적이다. z가 낮은 셋(355~400mm)은 전부 15mm 안으로
들어왔다. **기록은 저장소 쪽 파일로 실제 남았다**(젯슨 20줄 중 마지막 5줄을 이
저장소의 `docs/시험기록/move-to-point-2026-09-18.jsonl`에 수동으로 이어 붙였다 —
T58이 아직 안 고쳐진 경로 문제라 이번에도 손으로 했다, T58 note 참고).

⚠ **뒷정리에서 새 결함을 하나 더 확인했다(T60 보강)** — `job=park`(대기 자세
60,65,0,-100,6)이 **A자세에서만 막히는 게 아니다.** 트라이얼5가 끝난 자세에서
시작해 3단계로 점점 목표에 가깝게(작은 `job=stage`로 수렴시켜) 남은 차이를
pan+2.5·lift+12.4·elbow+6.2·wristflex+15.2·roll-4.7°까지 좁혔는데도 **네 번
전부 `wrist_flex한계`로 거절됐다.** arm_stage.py가 관절을 하나씩 순서대로
움직이는 방식이라 wrist_flex 쪽 세그먼트가 |r|을 250mm대로 밀어 올리는 것으로
보인다 — 목표(-100°) 자체가 이 팔의 이 경로 전략으로는 사실상 못 가는 자리일
가능성이 있다. 팔은 결국 `pan57.5 lift52.6 elbow-6.2 wristflex-84.8 wristroll10.7`
(TCP 143,224,208, r=266)에서 토크를 켠 채 멈췄다(대기 자세는 아니지만 낮고
안정적) — 그리고 집게는 100→98.6으로 열어 두었다.

**원인**: prep(T49)이 A자세(signed_r -221.7mm)에서 18걸음으로 뻗으려 했는데
**2번째 걸음이 16.3°를 요구해 관절 한 걸음 상한(12°)에 걸려 거절됐고, 그 걸음을
쪼개는 로직이 없어("쪼개는 쪽이 걸음마다 실제 자세를 되읽어야 하므로 여기서
대신 하지 않는다") 그대로 포기했다.** 팔은 물리적으로 거의 안 움직였다(뻗기 전후
관절각 차이 ≤0.6°, TCP 그대로 r≈-221~-225mm) — 그래서 5회 전부가 **표적의 도달
가능성과 무관하게 자세 가드에서 막혔다**. 이 0/5는 §24-1이 실측한 "팔이 못 드는
자리"와도 무관한 **세 번째 원인**(가드→걸음상한→표적범위 다음의 **prep 자체 결함**)이다.
→ 새 결함 **T56**(builder, p1: prep의 걸음 쪼개기를 실제로 구현하거나, 실패 시
남은 걸음을 더 작은 스텝으로 재시도하게 한다).

**부수 발견**: click-server의 `park` job 기본값(`arm_stage.py --target=60,65,0,-100,6`)도
같은 시작 자세(A자세, r=-225.4mm)에서 안전검사에 걸려 **움직이지 않았다**(경로 중
wrist_flex·elbow_flex 구간이 한계에 닿음) — 팔은 다치지 않았지만 "대기 자세로
돌린다"는 표준 동작이 지금 이 A자세에서 막힌다는 뜻이라 별도로 적어 둔다(우선순위는
낮음, 새 작업 안 만듦 — T56이 고치는 뻗기 로직과 같은 뿌리일 가능성이 있어 T56에서
같이 보는 편이 낫다). 마무리는 ROS 컨테이너를 내려 포트를 돌려준 뒤 click-server의
`grip` job(value=100)으로 집게를 열어(rc=0 확인) 끝냈다 — 팔 자세 자체는 시작할 때와
사실상 같다(대부분의 관절이 A자세 값에서 ≤4° 안).

### T63 실기 5회×2 (2026-09-18, 사이클 52 tester) — **seed 1: 0/5 · seed 2: 0/5**

T57의 3/5(seed=0)가 우연인지 보려고 같은 절차(docker compose run → stage1
arm:=true → `move5_check.py --seed N`, 인자 없음=기본 뻗기)로 두 판을 더 돌렸다.
**둘 다 5회 전부 실패했다.**

⚠ **한 가지가 T57과 다르다 — 시작 자세.** T57은 A자세(signed_r≈-225mm, prep이
뻗어야 하는 상태)에서 시작했다. 이번 두 판은 **사이클51 builder(T61)가 "붙든 채"
남긴 이미 뻗은 자세**(TCP -146.9,161.6,438.1 · shoulder_lift 79.1)에서 시작했다
— `prep.needed=false`(signed_r 227.3mm·348.0mm, 이미 가드 밖)로 5회 내내 한
번도 뻗지 않았다. 즉 이번 두 판은 "뻗기 이후 첫 이동"이 아니라 **"이미 뻗은
자세에서 표적으로 이동"**을 시험한 것이다 — T57과 조건이 정확히 같지 않다.

**seed=1** (`load_limits=config.ARM_LOAD_R_MAX`):

| 회 | 설자리 r | 설자리 z | 오차mm | 판정 |
|---|---|---|---|---|
| 1 | 251.9 | 405.2 | 117.72 | 실패(elbow_flex −128, 한계 ±98) |
| 2 | 299.2 | 368.3 | 16.08 | 실패(15mm 문턱을 1mm 넘김 — 거의) |
| 3 | 235.0 | 395.7 | 172.88 | 실패(elbow_flex −109) |
| 4 | 274.5 | 371.1 | 163.68 | 실패(elbow_flex −122) |
| 5 | 262.6 | 411.3 | 189.43 | 실패(elbow_flex −112) |

**seed=2** (`load_limits=config.ARM_LOAD_R_MAX`):

| 회 | 설자리 r | 설자리 z | 오차mm | 판정 |
|---|---|---|---|---|
| 1 | 269.3 | 337.0 | 89.78 | 실패(elbow_flex −107) |
| 2 | 283.3 | 373.6 | 99.68 | 실패(elbow_flex −101) |
| 3 | 272.9 | 385.8 | 98.27 | 실패(elbow_flex −119) |
| 4 | 209.3 | 455.5 | 203.61 | 실패(elbow_flex −108) |
| 5 | 218.3 | 425.2 | 199.45 | 실패(elbow_flex −108) |

**T62(z 처짐 가설)로는 설명 안 된다.** 이 10회 전부가 T40/T62가 말한 "z가 높아서
lift가 처진다"가 아니라 — 실패 사유가 매번 **elbow_flex가 음수 한계(−98°)에
걸린다**(−101~−128)는 것으로 통일돼 있다. z가 상대적으로 낮은 표적(seed1 회2
z=368, seed2 회1 z=337)도 실패했다 — T57에서 성공했던 z 355~400mm대와 겹치는데도
막혔다. **읽히는 것**: 실패 원인은 "표적의 z"가 아니라 "직선 경로가 첫 걸음에서
관절공간으로 돌아가는데, 그 첫 걸음이 **현재(이미 뻗은) 자세에서** elbow_flex를
한계 밖으로 요구한다"는 것 — 즉 **시작 자세가 이미 팔을 elbow_flex 여유가 거의
없는 자리로 밀어둔 상태에서, 어느 표적으로 가든 첫 스텝이 그 한계를 넘는다.**
T57은 A자세(elbow_flex 여유 있음)에서 뻗은 뒤 이동했고, 이번은 **뻗은 뒤 한 번도
원위치로 안 돌아온 자세에서 또 이동**한 것이 다르다 — "뻗은 채로 여러 번 옮겨
다니면 누적 자세가 나빠지는가"는 이번 기록만으로 못 가른다(10회 모두 같은
시작조건에서 나왔다).

**planner에게**: T57의 3/5는 여전히 "믿을 수 있는 유일한 완주"로 남지만, 이번
두 판은 **같은 조건의 재현이 아니다**(시작 자세가 다르다) — "3/5가 우연인가"에는
아직 답을 못 냈다. 공정한 비교를 하려면 **매 판을 A자세(또는 같은 알려진 시작
자세)에서 시작**해야 한다 — 지금처럼 이전 사이클이 팔을 어디에 붙들어 두고
끝내느냐에 따라 다음 tester의 판이 달라진다. T62(가르는 양)도 이 10줄을 "z 처짐"
줄과 나란히 넣어 다시 봐야 한다 — elbow_flex 한계가 더 강한 신호일 수 있다.

기록은 젯슨 30줄(10줄 신규) → `record_pull.py --date 2026-09-18`로 저장소
63→73줄에 이어붙였다(겹침 20줄 확인). 마무리: ROS 컨테이너 정리 후 click-server
복귀, 팔은 낮고 안정된 자세(TCP 329.9,-1.8,252.7)에서 grip 100(열림) 확인, 서비스
원상(click-server·depth-cam·astra-cam active / tomato-voice·controller-drive
inactive).

### T56 (2026-09-18, 사이클 44 builder) — **걸음이 낡는다**: 그 0/5의 원인을 코드에서 끊었다

위 §T54의 "쪼개는 로직이 없다"는 진단은 **절반이 틀렸다**. `escape.plan`은 이미
12° 이하로 균등히 쪼갠다(18걸음이 그 증거다). 틀어진 것은 **걸음이 낡는다**는 쪽이다:

- 받는 쪽(`arm_source.move_joints_deg`)은 지령을 받을 때마다 **지금 실제 자세에서
  다시 `plan`** 하고 두 걸음 이상이면 거절한다.
- 보내는 쪽(`move5_check.run_prep`)은 **처음 자세에서 짠 18걸음을 그대로 차례로**
  보냈다. 한 걸음(≈12°)을 보내면 팔은 중력으로 4~5° 못 미친 자리에 선다(§22 실측
  4.0~4.9°). 그러면 2걸음째 목표는 실제 자세 기준 **12 + 4.3 = 16.3°** — 기록에
  남은 그 숫자가 정확히 이 합이다.

**고친 것**: 되읽고 다시 짜는 일을 `hardware/escape.py`의 **`walk()`** 하나가 한다
(규칙을 셋이 베끼지 않는다는 이 모듈의 원칙 그대로). 걸음마다 `measure()`로 실제
자세를 읽고 남은 길을 다시 `plan`해 **첫 걸음만** 보낸다. 끝나는 조건은 셋이다 —
도착(`ARRIVE_DEG` 1.0°), **더 안 줄어든다**(`MIN_GAIN_DEG` 0.5°, "무언가에 걸렸다"의
진짜 증거), 예산 소진(계획 걸음수×3). 보내는 걸음은 상한보다 **1° 작게**
짠다(`SEND_MARGIN_DEG`) — 받는 쪽이 따로 읽은 자세가 한 박자 늦으면 꽉 찬 12°가
12.x°로 읽혀 거절되기 때문이다.

`run_prep`은 이제 `walk()`를 부르고, **`extended`의 뜻도 바꿨다**: "걸음을 다
보냈다"가 아니라 **"가드를 넘었다"**(`signed_r_after >= GUARD_MM`)다. 처짐으로 목표에
1~2° 못 미쳐도 가드 밖이면 시험은 돌아가고, 반대로 걸음을 다 보내고도 가드 안이면
5회가 전부 거절당한다 — 그 **거짓 0/5**를 기록에서 가려내는 것이 이 표지의 일이다
(거짓 0/5를 아예 안 남기는 것은 T59).

**검증**(PC, 팔 없이): `ros_selfcheck` [뻗기]에 가짜 팔(`_SagArm`: 4.3° 처짐 +
`arm_source`와 같은 거절 규칙)을 두고 6종을 더했다 — ①낡은 걸음을 그대로 보내면
**2걸음째가 15.8°로 거절된다(병의 재현)** ②`walk`는 같은 팔에서 36걸음으로 **한 번도
거절당하지 않고** 완주한다 ③보낸 걸음이 상한에 꽉 차지 않는다 ④완주한 자리는
`signed_r −221 → 219mm`로 가드(90mm)를 넘는다 ⑤안 움직이는 팔에서는 예산 안에
멈추고 **실패로 남는다**(뻗은 척 금지) ⑥`run_prep`이 그 되읽기를 실제로 쓴다.
돌연변이 4건 전부 FAIL 확인: 되읽기 제거(3검사)·여유 0(1)·진전없음 판정 제거(1)·
`run_prep`을 옛 루프로 되돌림(1). PC 4종 **45 / 102 / 249 / 91 전부 RC=0**.

⚠ **실기 확인은 아직 없다**(tester T57). 젯슨에도 같은 파일을 올려 뒀고
(`escape.py` md5 `3f0aa4c6…`, `move5_check.py` `07b1c385…`) 그 [뻗기] 6종은
젯슨에서도 초록이다 — 그러나 그것은 **가짜 팔 위의 초록**이다.

**§T54 기록의 소재 — 사라진 게 아니라 젯슨에 있다(T58에 넘김)**: 사이클42의 5줄은
젯슨 `~/tomato-picker/docs/시험기록/move-to-point-2026-09-18.jsonl`(15줄)에 그대로
있다. `prep.detail`이 5줄 모두 `"2/18걸음 거절 — 한 번에 16.3°는 너무 크다(관절 상한
12°)"`다. 저장소(PC, 52줄)와 **다른 계보의 파일**이라 그냥 덮으면 안 된다 — 합치는
방법은 T58이 정한다.

**§T54 기록을 가져왔다 — 그 5줄은 이제 저장소에 있다(T58, 사이클 46)**: 사이클42의
5회는 **한동안 jsonl에 한 줄도 없었다**(산문에만 있었다). 이제 저장소
`docs/시험기록/move-to-point-2026-09-18.jsonl`에 붙어 있고(57→62줄), 5줄 모두
`stage="pose"` · `prep.detail="2/18걸음 거절 — 한 번에 16.3°…"`다. 가져온 명령은
`python ros2/tools/record_pull.py --date 2026-09-18`이고, 두 번 돌려도 0줄이 붙는다.

왜 도구가 따로 필요했는가 — **젯슨 트리는 git 저장소가 아니다**(실측:
`git rev-parse` → "not a git repository"). 거기 쓴 줄은 커밋될 길이가 없다. 게다가
같은 이름의 파일이 두 곳에서 따로 자라서(그때 저장소 57줄 / 젯슨 20줄) **덮어쓰면
한쪽을 잃는다.** 그리고 글자 해시만으로는 못 가른다 — 사이클35의 10줄은 저장소에만
사람이 `cycle: 35`를 덧붙여 놔서 **같은 시험이 다른 줄로 보였고**, 그대로 붙였다면
10줄이 겹쳐 붙었을 것이다(실측으로 그렇게 나왔다). 그래서 신원은 글자가 아니라
`(trial, dry_run, commanded, ok, stage, error_mm, detail, t)` 칸으로 본다.

앞으로 같은 일이 되풀이되지 않게 `move5_check`가 스스로 말한다: 기록 자리가 git
저장소 밖이면 시작 줄과 끝 줄에 경고와 **가져오는 명령 한 줄**을 찍고, 아예 쓸 수
없는 자리면 **팔을 움직이기 전에** rc=2로 멈춘다(기록 없는 5회는 다음 사이클에
아무것도 아니다). 줄마다 `record_home`과 `t`(시각)가 붙는다. 강제 =
`ros_selfcheck`의 **[기록]** 19종.

**연습과 기록은 이제 같은 파일이 아니다(T43, 2026-09-18).** 반대쪽 구멍이 하나 더
있었다 — 기록을 *못 남기는* 것만 병이 아니라 **없던 일이 기록에 남는 것**도 병이다.
사이클35의 빌더가 도구를 고친 뒤 `--dry-run --n 2`를 스모크 테스트로 한 번 돌렸더니
그날의 진짜 시험기록에 연습 줄 2개가 섞였다(그때는 `git checkout`으로 되돌렸다).
`TOMATO_RECORD_DIR`이 있었지만 **기억해야 쓰는 것은 없는 것과 같다.** 그래서 기본값을
뒤집었다: `--dry-run`은 임시 디렉터리(`<temp>/tomato-move5-dry/`)에 떨어지고, 그 줄은
`record_home="practice"`로 표시된다(젯슨의 `volatile`과 뜻이 다르다 — 회수할 것이
없다). 연습을 진짜 기록에 남기려면 `--record`로 **말해야** 한다. **실기(`run_real`)의
기본은 그대로다.** 검사는 갈림만 보지 않고 실제로 `--dry-run`을 돌려 진짜 파일의
바이트가 그대로인지 확인한다 — 돌연변이로 갈림을 없애면 그 검사가 실제로 죽고
(4종 FAIL) 진짜 기록에 연습 2줄이 실제로 섞였다(2026-09-18 실측, 되돌렸다).

**§T54 부수 발견(park)은 같은 뿌리가 아니다**: `arm_stage.py`는 `escape`를 안 쓴다
(자체 `STEP_DEG = 8.0` + 관절 하나씩 옮기는 자체 경로 계획, 버스에 직접 쓴다). 막힌
것도 한 걸음 상한이 아니라 **자기 안전검사가 경로 전체를 시작 전에 거절**한 것이다
→ 따로 작업으로 올렸다(T60).

### 기준4 채점표 — 626종 전수 무결성 및 "절대 실패할 수 없는 검사" 감사 (2026-09-20, 사이클 448)

기준4 = "PC 자체검증 도구 전부 통과 (All RC=0)."
09-18 감사 T39(427종) 이후 199종이 신설되어 2026-09-20 현재 총 626종에 달한다.
AST 및 코드 정적 분석을 통해 전수 626개 검사의 무결성을 감사했다:

| 검사 도구 | 검사 수 | 주요 검증 영역 | 무결성 감사 결과 |
|---|---|---|---|
| `tools/handeye_check.py` | 45종 | 손-눈 보정 수학(fixed/on_arm), 잡음 내성, 축 단위, Intrinsics 핀홀/왜곡, Rigid 변환, tool_frame 규약 | ✅ 항등식·자기비교 0건, 예외 삼킴 없음. 모든 assertion이 실제 수렴 및 잔차 임계값 검증 |
| `tools/eye_check.py` | 102종 | 다중 카메라(d405/astra), 노출/게인 제어, 발행 주기(fps), 겨냥 시효(0.35s) 가드 | ✅ 노출/게인/시효 경계 조건과 실패 반환 메시지 완벽 검증 |
| `ros2/tools/ros_selfcheck.py` | 386종 | 레거시 경계, 기하·URDF xacro 기본인자(25종), TF 마운트(19종), 모터/보드 계약, 깊이 처리, 역기구학, 손-눈 15mm 게이트, 표적/경로/되먹임 5/5, 시험기록 격리, 줄끝(LF), 서비스 충돌, 패키지 의존성, LoadLimits 마운트평면/타입가드(7종), docker-compose 장치요건(2종) | ✅ AST 검사 결과: `check(..., True)` 2건은 허용 모듈 순회 표시용이며 금지/미지 의존 시 `check(..., False)`로 즉각 실패 강제됨. 파일 부재 조건은 상위 `check(os.path.exists)`로 방어됨. metrologist 4종 및 auditor LoadLimits 타입가드/기하한계 3종 추가(379→386) |
| `tools/arm_cartesian_check.py` | 93종 | 직교 좌표 제어, 관절 보간 대체 경로, 처짐 되먹임(settle) 수렴/포화 | ✅ 복합 경로 및 되먹임 루프 전 단계 실측치 오차 검증 |
| **합계** | **626종** | **All RC=0 (All Green)** | **절대 실패할 수 없는 검사(무효 검사) 0건 확인** |

#### 돌연변이 및 오류 방어 시험 결과 (사이클 448 실측)
1. `tomato_robot.urdf.xacro`의 `pan_min_deg`를 `-110.0` → `-115.0`으로 5° 변조:
   - `FAIL tomato_robot.urdf.xacro 기본 인자 25종이 so101_geometry.yaml과 일치한다` 즉시 포착, **RC=1 반환 확인**.
2. `so101_geometry.yaml`의 `shoulder_pan`을 `[-110.0, 110.0]` → `[110.0, -110.0]`(역전)으로 변조:
   - `FAIL so101_geometry.yaml의 limits_deg가 6개 전 관절의 유효 범위(min < max)를 정의한다` 즉시 포착, **RC=1 반환 확인**.
3. `LoadLimits(310.0, "테스트")` 위치 인자 오지정 시:
   - `TypeError: z_max는 숫자여야 한다 (got str: '테스트')` 즉시 발생 및 `_raises` 포착, **위치 인자 오지정으로 z_max에 source 문자열이 주입되는 런타임 버그 방어 실측 검증**.

### 1단계 (`ros.1`) — 지금 여기

| | 상태 |
|---|---|
| URDF(xacro) + robot_state_publisher | ✅ |
| `/joint_states` (proxy/direct 두 길) | ✅ |
| `/cmd_vel` → 보드 (`C` 계약 + `V` 레거시) | ✅ (duty 곡선은 미실측) |
| D405 → `/fruits` (3D, 못 믿을 깊이는 **거절**) | ✅ |
| 손-눈 보정 (수집·풀이·static TF·잔차) | ✅ |
| `/arm/move_to_point` (TF → IK → 이동) | ✅ |
| ROS 없이 도는 자체검증 | ✅ `ros2/tools/ros_selfcheck.py` (386종, PC에서 — 2026-09-20 감사 재확인. 늘어난다: 계측규약·타입가드 검사군 추가) |
| 손눈보정 수학 자체검증 | ✅ `tools/handeye_check.py` (45종, PC에서) |
| **젯슨에서 실제 빌드** | ✅ 2026-08-28 — 도커 이미지 3.02GB, `colcon build` 6패키지 1분 12초 |
| **로봇 위에서 TF 확인** | ✅ `bringup_check.sh` 0~2단 — RSP가 만든 TF가 `kinematics.forward()`와 **0.000mm** 일치 (7자세) |
| **실물 팔 직결 확인** | ✅ `arm_check.sh` 3~4단 — ROS가 포트를 직접 잡고 실물 관절을 읽는다. 그 TF가 **레거시 대시보드가 보고한 집게 좌표와 0.03mm 일치** |
| **카메라·검출 확인** | ✅ `cam_check.sh` 5~6단 — D405 컬러 27fps/깊이 26.7fps, 정렬 848x480 16UC1, `/fruits` 26.5Hz 발행 (팔 없이 돈다) |

**1단계에서 일부러 안 한 것들**

- *`C`(물리 단위) 전송* — 계산·검증은 다 돼 있지만 보낼 링크가 없다.
  `MotorLink`의 재전송 스레드가 `V`를 되풀이하므로 `C`와 섞으면 서로를 지운다
  (보드계약 §5.1). 물리 단위 보드가 생기면 계약 v2 전용 링크를 만들어 물린다.
  그때까지는 **거절**한다 — 조용히 duty로 내려가지 않는다.
- *오도메트리(`/odom`)* — 엔코더가 없다. 없는 것을 0으로 발행하면 "안 움직인다"가
  아니라 **"움직이지 않았다는 거짓말"**이 된다. 보드계약의 `enc`가 1이 되면 그때.
- *집게 관절값 읽기* — 대시보드 status가 안 준다. URDF에는 있으므로 0으로 채우고,
  파라미터 이름(`gripper_deg`)으로 "모른다"는 사실을 남겼다.

### 2단계 (`ros.3`) — 시각 서보잉

1단계가 끝나도 남는 오차가 있다. **링크 길이·영점·손눈보정의 오차가 전부 더해져
집게 끝에 나타난다.** 각각 몇 mm씩이어도 합치면 열매 반지름(15~20mm)을 넘는다.

그래서 2단계는 **열려 있는 사슬을 닫는다**:

```
집게에 붙은 마커를 카메라가 본다  →  "지금 집게가 실제로 여기 있다"
목표(열매)와의 차이를 계산        →  그 차이만큼만 조그
                                     (기구학의 절대 정확도가 필요 없어진다)
```

- 필요한 것: 집게 마커(1단계의 보정용 마커를 그대로 쓴다), 오차→조그 변환,
  수렴 판정, **발산 감지**(이 저장소에서 흔들기가 발산한 전례가 있다 —
  [개발노트 2026-08-09](개발노트-2026-08-09.md)).
- ⚠ 조그 크기가 서보 분해능(0.088°/틱) 아래면 물리적으로 0이다. `cartesian.py`가
  이미 그 크기를 거절하므로, 서보잉 루프는 **거절을 수렴 실패로 오해하면 안 된다**
  (거절 = "더 못 줄인다" = 수렴 종료).

### 3단계 (`ros.4`) — URDF + MoveIt

- `ros2_control` 하드웨어 인터페이스로 팔을 감싼다(지금은 서비스 호출).
- MoveIt2 설정 + 줄기·지지대를 충돌 객체로.
- ⚠ **5축이라 6자유도 목표는 못 푼다.** MoveIt에 6D 목표를 주면 계속 실패한다 —
  위치 + 접근 방향만 구속하는 방식으로 목표를 세워야 한다
  ([`kinematics.py`](../src/tomato_picker/hardware/kinematics.py) 머리말 참고).

---

## 4. 레거시 의존 졸업표

새 계통이 옛 계통의 **껍데기**가 되지 않게 하는 표다. 선은 *새 코드냐 옛 코드냐*가
아니라 **계산이냐 소유냐**로 긋는다.

> **계산과 실측값은 공유한다. 장치와 상태의 소유권은 ROS가 가진다.
> 웹 서비스에는 붙지 않는다.**

기구학을 ROS 쪽에 다시 쓰면 **같은 팔을 두 계통이 다르게 믿게** 된다 — 이 저장소가
반복해서 비싸게 배운 실패다. 그러니 순수 함수를 쓰는 건 "레거시를 따라가는 것"이
아니라 라이브러리를 쓰는 것이다(`math`·`numpy`를 쓰는 것과 같은 층).

| 무엇을 | 어디서 쓰나 | 판정 | 언제 끊나 |
|---|---|---|---|
| `kinematics` (import: `math`) | `arm_node` | **공유** | 안 끊는다 |
| `handeye` (import: `numpy`) | `handeye_node`, `detect_node` | **공유** | 안 끊는다 |
| `cartesian` (영점·환산·안전) | `arm_node`, `arm_source` | **공유** — 이 팔의 실측값은 한 벌 | 안 끊는다 |
| `eye.EyeConfig` (`~/arm_eye.json`) | `handeye_node` | **공유** — 이 카메라의 보정도 한 벌 | 안 끊는다 |
| `ports`, `servo_probe`, `config` | `follower_io` | **공유** — 유틸·상수 | 안 끊는다 |
| `motor_link` (주행 보드 드라이버) | `cmd_vel_node` | **과도기** | `v2.0.0-ros.4` — `ros2_control` 하드웨어 인터페이스로 승격 |
| `lerobot.SOFollower` (모터 버스) | `follower_io` | **과도기** — import 하나에 **torch 607MB**가 딸려온다(실측 3.5초) | `v2.0.0-ros.4` — `scservo_sdk` 직결. ⚠ 그때 정규화(-100..100) 규약을 lerobot과 똑같이 맞춰야 한다(안 그러면 영점의 뜻이 달라진다) |
| `arm.LerobotArm` | — | **금지** (ros.1에서 끊음) | 끝 |
| `voice.*` (웹 서비스) | — | **금지** | 끝 |
| `harvest.SceneModel` | — | **금지** — 새 계통이 없애려는 그것이다 | 끝 |
| `vision.*` | — | **금지** — 검출은 ROS 토픽으로 | 끝 |
| `arm_mode:=proxy` (HTTP) | 브링업 확인용 | **과도기** | `v2.0.0-ros.3` — 삭제 |

### 강제하는 방법

`ros2/tools/ros_selfcheck.py`의 **[경계]** 검사가 `ros2/src`의 모든 `tomato_picker.*`
import를 모아 이 표와 대조한다. 금지 목록에 걸리거나 **표에 없는 것이 나오면 실패**한다
— 새 의존을 들일 때 판정을 적게 만드는 것이 목적이다.

웹 서비스는 import가 아니라 HTTP로 붙으므로 같은 검사가 **기본값**을 본다:
`arm_mode`가 `direct`인지, 보정 경로가 비어 있는지(= `~/arm_eye.json` 한 벌인지).

### `proxy`가 왜 기본이면 안 되는가

이 계통을 만든 까닭 중 하나가 **시간**이었다(§ROS를 쓰는 이유 ②). 깊이 프레임과
관절값은 다른 시각의 데이터라 *그 프레임을 찍은 순간의* 팔 자세로 풀어야 한다.
`proxy`는 남의 프로세스에 10Hz로 물어본 값이라 "언제의 자세인가"가 흐릿하고,
대시보드가 바쁘면 `stale`까지 온다. 그 위에 TF를 그리면 팔이 실제와 다른 곳에
그려지고, 그 위에서 계산한 좌표가 **전부** 틀린다.

포트가 안 열린다고 `proxy`로 도망가지 말 것. 답은 `systemctl stop tomato-voice`다.

### ⚠ 장치는 셋 다 배타적이다

| 장치 | 레거시가 잡는 것 | ROS가 잡는 것 |
|---|---|---|
| `/dev/ttyACM0` (팔) | `tomato-voice.service` | `arm_node` (`direct`) |
| `/dev/ttyUSB0` (주행 보드) | `controller-drive.service` | `cmd_vel_node` |
| **D405** | `depth-cam.service` (`/dev/shm`) | `realsense2_camera` |

세 번째가 처음에 문서에 빠져 있었다. 팔·바퀴만 신경 쓰고 카메라를 안 껐다가
`realsense2_camera`가 조용히 못 여는 일이 생긴다.

### `v2.0.0` 통과 조건

**`tomato-voice`·`depth-cam`·`controller-drive`를 전부 끈 채로 1단계 전체가 돈다.**
그때 레거시 의존은 위 표의 "공유" 줄만 남는다 — 그건 라이브러리이지 계통이 아니다.

---

## 5. 다음에 할 일 (`ros.2`로 가는 길)

### ① 링크 길이와 마운트를 **자로 잰다**

`ros2/src/tomato_description/config/so101_geometry.yaml`의 값은 도면 근사치다.
FK와 IK가 같은 틀린 값을 쓰므로 조그는 방향만 맞고 크기가 어긋나며, **절대 좌표
이동은 그만큼 통째로 틀린다.**

재는 법은 [`kinematics.py`](../src/tomato_picker/hardware/kinematics.py)의
`ArmGeometry` docstring에 있다. 재고 나면 `ros_selfcheck.py`가 코드 기본값과의
불일치를 알려 주므로 **양쪽을 같이 고쳐야 한다**(그게 그 검사의 목적이다).

### ② 손-눈 보정을 실제로 한다

**지금 하드웨어는 `on_arm`이다** — D405가 5번 관절(wrist_roll) **뒤**, 집게 바로 위에
붙어 집게 앞을 본다. 컬러 화면 아래에 보이는 검은 덩어리가 그 집게다. 그래서
마커를 **책상에 고정**하고 팔 자세를 크게 바꿔가며 8회 이상 찍는다(집게에 붙이는
것은 `fixed`용이다). 차체 고정용 ORBBEC Astra Pro는 아직 안 왔다.

절차는 [`ros2/docker/README.md`](../ros2/docker/README.md) §3.
잔차 RMS 15mm는 **열매 지름이 30~40mm라서** 나온 숫자다 — 그보다 크면 집게가
헛집는다. 넘으면 표본을 더 넓게 흩어서 다시 찍어라.

#### 보정 **전에** 깊이 사슬부터 재라 — `ros2/tools/target_check.py`

손-눈 보정은 "카메라 좌표 → 팔 좌표"를 푼다. 그 **앞 단계**("픽셀 + 깊이 →
카메라 좌표")가 틀려 있으면, 보정이 그 오차를 흡수해서 **잔차는 작게 나오고
팔은 헛집는다.** 그러니 아는 길이로 먼저 재야 한다.

벽에 흰 종이를 붙이고 검은 점 네 개를 **아는 간격**으로 찍으면 된다
(2026-08-31 실측: 가로 100mm, 세로 174.5mm, 카메라에서 390mm).
`depth-cam.service`가 도는 채로 젯슨에서 그냥 돌린다 — ROS도 컨테이너도
서비스 정지도 필요 없다.

    ~/.venvs/vision/bin/python ros2/tools/target_check.py

2026-08-31 결과:

| 잰 것 | 기대 | 실측 |
|---|---|---|
| 위 가로 / 아래 가로 | 100.0mm | 99.8 / 99.0mm |
| 왼 세로 / 오른 세로 | 174.5mm | 173.8 / 173.5mm |
| 대각선 ↘ / ↗ | 201.1mm | 199.9 / 200.2mm |
| 종이 평면성 | — | 중앙값 0.67mm · 95%tile 2.14mm |
| 벽 법선 ↔ 광축 | — | 2.1° |

**평균 편향 −0.73mm = 스케일 오차 −0.73%.** 1% 안쪽이므로 이 사슬은 믿어도 된다.
2%를 넘으면 내부파라미터나 깊이 단위를 의심해야 하고, 그건 보정으로 안 없어진다.

⚠ **검은 점 위의 깊이를 그대로 쓰지 않는다.** 검정은 IR을 흡수해 구멍이 난다.
   흰 종이의 깊이로 **평면을 맞추고** 그 평면과 점의 시선이 만나는 곳을 쓴다.

⚠ **`depth_scale_mm`이 1.0이다** — [`docs/depth-camera.md`](depth-camera.md)는
   0.1이라고 크게 경고해 뒀지만 지금 이 카메라는 1.0을 보고한다. 위에서 잰 길이가
   맞게 나오므로 **1.0이 옳다.** 코드가 이 값을 카메라에서 읽게 해 둔 덕에
   아무것도 안 깨졌다 — 상수로 박았다면 거리가 통째로 10배 틀렸을 것이다.
   (그 문서의 숫자는 다른 개체이거나 옛 설정으로 보인다. 어느 쪽이든
    **읽어서 쓰는 게 맞다**는 것이 이 사례로 다시 확인됐다.)

#### 보정이 맞았는지 눈으로 거르는 법 — `camera_at`

`eye.py`의 스냅샷이 `camera_at`을 준다(`on_arm`이면 **집게 기준 카메라 위치**).
실물 배치(2026-08-31 사진)는 카메라가 집게 무는 지점에서 **뒤로 약 7~8cm,
위로 약 7~8cm**다. 풀린 값이 그 근처가 아니면 자릿수부터 틀린 것이다 —
잔차가 작아도 믿지 마라(표본이 한 뭉치에 몰리면 그럴 수 있다).

그 배치의 부수 효과 하나: **파지 순간에도 열매가 카메라에서 약 108mm**다
(스탠드오프 45mm면 143mm, 접근 시작 150mm면 238mm). 전부 D405 유효범위
70~500mm 안이다 — 손목 장착이라 거리가 자동으로 좋은 구간에 들어온다.
(`near_frac`이 낮게 나왔던 건 팔이 접혀 방을 보고 있었기 때문이지 배치 문제가
아니었다.)

#### ⚠ `on_arm`에서 두 계통의 "도구 좌표계"가 다르다 — wrist_roll을 고정하라

보정 파일은 한 벌이지만, `on_arm`에서 **그 값을 해석하는 방식이 서로 다르다.**

| | 도구 좌표계를 어떻게 만드나 | wrist_roll |
|---|---|---|
| 레거시 `eye.py` | `handeye.tool_frame(pose)` (`eye.py:339`, `:390`) | **무시한다** — `tool_axes`가 pitch·방위각만 쓴다 |
| ROS `handeye_node` | `tf2`로 `arm_base → tool0` 조회 | **포함한다** — URDF의 wrist_roll 관절이 들어 있다 |

카메라는 물리적으로 wrist_roll **뒤**에 붙어 있으므로 roll과 **같이 돈다.** 즉 TF
쪽이 물리적으로 옳고, `tool_frame()` 쪽은 roll이 보정 때와 다른 순간부터 틀린다.
(`tool_frame`의 docstring도 "손목 장착 보정 중에는 roll을 고정하라"고 적고 있다 —
그건 보정 중의 주의사항이지만, 같은 오차가 **운용 중에도** 그대로 나타난다.)

**지금 지킬 규칙:** 보정할 때와 열매를 집을 때 **wrist_roll을 같은 값으로 둔다.**
그러면 두 계통이 같은 답을 낸다.

**근본 해결:** `handeye.tool_frame()`이 roll을 포함하게 고치면 된다(`fixed` 경로는
`tool_frame`을 쓰지 않으므로 영향이 없다). 그건 D405 통합 작업이 소유한 파일이라
여기서 건드리지 않았다 — **그쪽과 맞춰서 한 번에 바꿔야 한다.**

### ③ duty 곡선 재기 (`duty_measured: true`로 가는 길)

지금 `Ks=90`은 실측이지만 `Kv`는 아니다. 그래서 `/cmd_vel`의 m/s는 **방향과
비율만** 맞다. 재는 법:

1. 바닥에 1m 눈금을 그린다(또는 라인 카메라의 픽셀 이동을 쓴다).
2. duty를 120, 150, 180, 210으로 두고 각각 **2초씩** 직진시켜 이동 거리를 잰다.
   (0.12초 펄스로 재지 말 것 — 가감속 구간이 절반을 차지한다.)
3. `속도[mm/s] = 거리 / 2`. duty를 x축, 속도를 y축에 놓고 직선을 맞춘다.
   기울기의 역수가 `Kv`(duty per mm/s), x절편이 `Ks`다.
4. 회전도 같은 방법으로(제자리 360° 도는 데 걸린 시간).
5. `stage1.yaml`에 넣고 `duty_measured: true`.

⚠ **배터리 전압에 따라 곡선이 움직인다.** 그게 보드계약 v2가 폐루프로 가려는
이유고, 그때까지는 "완충 직후에 잰 값"이라는 단서를 달아 두는 것이 정직하다.

### ④ 기존 무대 학습 걷어내기 (`v2.0.0`에서)

`harvest.py`의 `SceneModel`은 좌표가 생기면 **필요 없어진다** — 나무 번호도
높이도 3D 좌표에서 바로 나온다. 다만 걷어내는 순간 음성 명령("2번 나무 위쪽")이
같이 죽으므로, 좌표 → 사람 말 변환을 먼저 만들어 둔 뒤에 지운다.
